provider: mediacrawler
mediacrawler_dir: ../MediaCrawler          # 相对项目根；不存在则降级 fixture
fixture_path: tests/fixtures/search_contents_sample.jsonl   # 降级用
mediacrawler:
  login_type: qrcode                       # 或 cookie（配 cookies）
  cookies: ""
  out_dir: data/raw                        # 真实采集 raw 落此（gitignore）
keywords_file: configs/keywords-留学辅导.yaml     # 词表资产外置（与 inline keywords/synonyms 互斥）
search:
  pages: 1
  limit: 20
  sort: time_descending
  window_days: 30
ranking:
  weights:
    note_count: 10
    keyword_hit: 5
    interaction: 0.01
    vertical: 10.0
    activity: 1.0
watchlist_file: configs/watchlist-留学辅导.yaml   # 账号资产外置（与 watchlist.manual 互斥）
watchlist:
  auto_top_n: 2
  max_total: 30   # 2026-07-03 用户拍板：上限由 10 提至 30（用量口径变更）
creator:
  notes_per_account: 10
selection:
  top_notes_per_account: 2
  half_life_days: 14
comments:
  enabled: true
  limit: 10
  report_top_k: 3
  fixture_path: tests/fixtures/comments.jsonl   # MediaCrawler 不可用降级 fixture 时使用
logging:
  level: info
  dir: data/logs
  file_enabled: true
export:
  out_dir: data/exports
