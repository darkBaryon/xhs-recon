---
title: 导出按运行归档
type: 需求
id: 导出按运行归档
status: 已交付
tier: 轻量级
created: 2026-07-02
---

## 背景

`data/exports/` 原为原地覆盖语义——换关键词再跑，上一次研究结果即被冲掉（raw/logs 均按 run 留档，唯导出层不留）。用户明确要求「结果不要覆盖，按时间保留」。

## 目标

导出落 `data/exports/<时间戳>/`（与 run 日志同一 compact 时间戳，可互相对上）；`data/exports/latest/` 软链恒指最新一次。latest 同名实体已存在或软链失败时仅 WARN 不拦管线（旁路口径）。

## 验收口径

同配置跑两次 → 两个独立目录、历史不覆盖、latest 指向新者（专项测试 2 个）；e2e 改用返回路径断言；ruff/pytest 全绿；README 回填；用户看 diff 点头合入（简化流，实施经用户明示授权由 Claude 执行）。

## 处置历史

- 2026-07-02 立案并同日实施（feat/export-archive；轻量级简化流）。
