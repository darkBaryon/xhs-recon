---
title: <简称> 期<N> 方案 v1
type: 方案
case: <过程文档名>
phase: <N，非分期功能固定 1>
version: 1
status: 草拟中
relates: [<需求 id 列表>]
created: <YYYY-MM-DD>
---

## 给用户的摘要

<≤10 行人话叙事；至少一组 before/after（现状 → 做完）；拍板清单（每项一句话利弊，无则明写「无」）；不用未定义私有术语>

## 1. 起点

`起点=<commit hash 或文件盘点>`——一切 diff、复测、对账以它为参照。

## 2. 现状与约束盘点

<写方案当时实测，带 文件:行号。必须含**复用盘点**：要新建的能力现有代码有无可复用出口？约束（只读边界/敏感字段/不碰范围）逐条列。>

## 3. 目标 / 非目标

**目标**（本期交付的功能点）：

**非目标**（显式列出不做的范围）：

## 4. 接口与数据设计

<新增/变更的对外接口签名、数据模型（dataclass/schema/表结构）、模块边界与依赖方向。开发方案的核心：契约定清，实施只是填充。>

## 5. 实现步骤

<小步、每步可独立 CI 全绿 commit；步骤号对应 commit message>

## 6. 测试策略

<单测/集成/验收用例；行为断言优先；列出验收用例的输入与期望输出；标注盲区>

## 7. 验收标准 + 检查清单 YAML

<机械可核验。参数表落同目录 `checklist.yaml`（预飞/评审共用唯一事实源），此处只写人话解释>

## 8. 风险与禁止事项

<本期风险；越界禁止项（如代理池/验证码/自动互动等本项目红线）>

## 9. 回滚口径

<未合主线前怎么弃子；已 commit 的去留由用户裁决>

---

<!-- 以下三章节由实施职责在实施完成后追加，追加齐备即流转「已实施」 -->

## 实施记录

<每步 commit hash + 一句话说明 + 验收标准逐项自查>

## 实施偏差记录

<与方案不符处，哪怕很小；无则写「无」。方案明示条目未执行也属偏差，必须登记>

## 蓝图验证备忘

<仅蓝图级分期功能必需；其余写「不适用」>

## Gate 决策结果

<仅 Gate 决策落地允许原地修订，逐处标注来源>
