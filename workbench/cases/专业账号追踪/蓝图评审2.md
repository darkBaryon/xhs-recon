---
title: 专业账号追踪 开发蓝图评审 #2
type: 方案评审
case: 专业账号追踪
phase: 0
round: 2
verdict: 通过
created: 2026-07-02
---

## 结论

verdict: **通过**。v1→v2 差量正中第 1 轮的 2 阻塞 + 4 建议：**阻塞1 关闭**——源需求验收口径确已回写（「creator_notes 按原样导出、topic_feed 仅收窗内（机械可验）」，处置历史留痕），蓝图 §4 三期验收与新口径逐条一致，不再有静默降级；**阻塞2 关闭**——契约新增 `account_profile.csv` 承载 vertical_ratio/recent_note_count/profile_score，旧 5 文件（含 account_rank.csv 7 列，亲核确认）声明不动，自相矛盾消除。4 条建议（core 边界、2 条新风险、下限钳制知情、`--sort_type` 缺省不覆盖）全部吸收，声称的外部事实引用（`client.py:618-649`、`core.py:133-134`）抽查属实。差量**未夹带**改端口签名/models/依赖方向/分期结构。残留 2 条非阻塞瑕疵（§2 图「+3 文件」未同步为 4 件；「互动降辅助」措辞被删对期3 可复算的隐含约束）记建议栏。

## 差量逐项核查（只评 v1→v2 增量）

我对 v1↔v2 做了归一化版本号后的整文 diff（`diff <(sed 's/v1/vN/g' 开发蓝图.md) <(sed 's/v2/vN/g' 开发蓝图v2.md)`）：实质差异全部落在——frontmatter status、摘要（After 行/拍板 2 条措辞）、新增「v1→v2 变更」节、§2 图 3 处标注、§3 事实清单 2 条知情项+契约 4 处、§4 三行验收口径、§5 新增 2 条风险+2 处措辞。逐条核：

### 阻塞1 关闭：验收口径回写 + 三期逐条对齐

- **回写真实发生**：亲核 `reports/专业账号追踪.md` 验收口径（第 41-42 行）——原「每账号最新 N 篇 published_at 均落窗内」已改为「watchlist 每账号最新 N 篇**按原样导出**（`creator_notes.csv`，不活跃账号可能出窗，属预期）」+「时间口径机械可验：**`topic_feed` 只收时间窗内笔记**（全部 `published_at` 落窗内），出窗/缺时间的笔记不入 feed、在报告计数」。与第 1 轮建议的「拉取后按窗过滤再导出（在 feed 层）」方向一致，且消解了「最新 N 篇 vs 均落窗内」的语义冲突。
- **处置历史留痕**：需求第 50 行「2026-07-02 验收口径回写（响应蓝图评审 #1 阻塞1）：…改为『creator_notes 按原样导出、topic_feed 仅收窗内（机械可验）』」。来源标注明确。
- **蓝图 §4 与新口径逐条一致**：期1 =「fixture 测试：窗过滤后 notes `published_at` **全部窗内**、缺时间归窗外并计数、衰减公式可复算（机械可验）」——比 v1 的「中位数+手动」实质收紧为机械可验，真实跑降为抽查最新性（诚实标手动）；期2 = 每账号最新 N 篇（手动验收），与「creator_notes 按原样导出、出窗属预期」口径相容，本就不该有窗内断言；期3 =「`topic_feed` 全部 `published_at` 窗内 + 出窗/缺时间**计数对账**（机械可验）」——需求的机械可验口径落在正确的一期。**无任何一期静默放宽，关闭。**

### 阻塞2 关闭：分项载体 account_profile.csv

- §3 导出契约从「新增 3 件」改为「新增 4 件」，新增 `account_profile.csv # account_id,nickname,vertical_ratio,recent_note_count,profile_score（期3，分项可复算）`——需求目标 2 的两个分项有了明确落点；需求验收第 41 行同步写明「落 `account_profile.csv`，可复算」，两份文档一致。
- **旧 5 文件确实装得下「不动」承诺**：亲核 `src/core/exporter.py:134-143`，account_rank.csv 恰 7 列（account_id/nickname/relevant_note_count/keyword_hit_count/avg_interaction/account_score/note_ids），v2 变更节「含 account_rank.csv 7 列」断言属实；契约明示旧 5 文件逐字节不变 + 非目标「不改旧文件格式与字段」不再被期3 交付顶穿。**矛盾消除，关闭。**
- 附带核对：watchlist.csv 从 v1 的 `account_id,nickname,source,score` 去掉了 score 列——第 1 轮正是指出「单一 score 列装不下分项」，分项统一挪到 account_profile.csv 后 watchlist.csv 回归纯名单语义，是对阻塞2 的合理收敛，非夹带改契约。

### 建议 4 条吸收核查

1. **core 边界（原质疑4）**：§2 图 watchlist 标注「只吃归一化 id」、parsers 标注「+主页 URL→user_id 归一化，期2」、§3 契约注释「归一化在 adapters.parsers（平台专属）；core.watchlist 只吃归一化 id」、§4 期2 交付「parsers 加 URL→user_id 归一化」（v1 此项在 watchlist 交付里）。**归属澄清到位，铁律「core 无 xhs」不再被顶。**
2. **新风险：置顶笔记（原质疑5）**：§3 知情条 +§5 风险条均已列，对策「notes_per_account 留余量 + topic_feed 窗过滤兜底 + 不做置顶识别（实测干扰大再议）」——兜底有定义。**事实抽核属实**：`../MediaCrawler/media_platform/xhs/client.py` 第 595 行 `get_all_notes_by_creator`、第 618 行 `while notes_has_more and len(result) < config.CRAWLER_MAX_NOTES_COUNT`、第 641-645 行 `remaining` 截断 `notes[:remaining]`——按主页分页顺序截断、无任何时间排序，引用行号 618-649 精确。且 v1 §3 的「最新优先」失实措辞在 v2 已删除。
3. **新风险：auto 样本不足（原质疑6）**：§5 新增「取实际数量入 watchlist，不硬凑，报告标注『auto 实际 K/N』」——行为已定义，不留实施自由裁量。
4. **下限钳制知情 + `--sort_type` 缺省不覆盖**：§3 知情条「search 流下限钳制 20（core.py:133-134）；creator 流无钳制」——**亲核属实**：`core.py:133-134` 在 `search()` 内 `if config.CRAWLER_MAX_NOTES_COUNT < xhs_limit_count(20): 抬到 20`；且 grep 全文 `CRAWLER_MAX_NOTES_COUNT` 在 core.py 仅 133/134/141 三处、全在 search 路径，creator 路径直达 client 无钳制，「notes_per_account: 10 按字面生效」成立。`--sort_type`「可选参数，缺省不覆盖」已进摘要拍板、§2 图、§3 config 注释（`sort 缺省不传→不覆盖 fork 配置`）、§4 期1 交付四处，口径一致。

### 无夹带核查：契约 / 分期 / 依赖方向

- **§3 契约**：端口 `fetch_creator_notes(account_ids, limit, collected_at) -> FetchResult`、search 签名不动、models 只增不改（AccountRank 两分项 default 兼容、WatchAccount 新模型）——与 v1 逐字一致，仅 Watchlist 条目→WatchAccount 的命名统一 + 归一化注释。**未借机改接口。**
- **§2 依赖方向**：铁律行字节一致；图内差异仅 3 处标注（watchlist 归一化 id、parsers 归一化、fork 可选/缺省不覆盖），无新层无新箭头。
- **§4 分期结构**：三期交付物/依赖前置不变；差异全是验收口径对齐（阻塞1）+ account_profile.csv 落期3（阻塞2）+ 期2 前置 spike 从 §5 显式化进验收列（非新增范围）。
- **§5**：新增 2 条即建议项，另 2 处为措辞微调（采集→拉取、删「日志/」），无实质变化。

## 可证伪质疑

1. **【已关闭】阻塞1——需求是否真回写、三期是否逐条承接？** 证伪测试：读 reports 第 41-42 行新口径 + 第 50 行处置历史；逐行比对 §4 三期验收列。结果——回写真实发生且标注来源，期1 窗过滤机械可验、期2 按原样导出无窗内断言、期3 topic_feed 窗内+计数对账机械可验，与新口径一处不差。关闭。
2. **【已关闭】阻塞2——分项载体是否存在且不顶穿旧契约？** 证伪测试：§3 契约 4 件新文件逐列读 + 亲核 exporter.py:134-143 旧 account_rank.csv 列集。结果——account_profile.csv 五列含两分项+综合分，旧 7 列文件无需动，需求验收同步写明载体。关闭。
3. **【已关闭】v2 新引的两条事实引用（client.py:618-649、core.py:133-134）是否属实？** 证伪测试：打开 fork 源文件比对行号与语义。结果——618/641/645 行截断逻辑、133-134 行 search 钳制均精确；并补核 creator 路径确无钳制（core.py 全文仅 3 处引用该配置、全在 search）。关闭。
4. **【已关闭】差量是否夹带改契约/分期/依赖？** 证伪测试：归一化整文 diff + §3 契约块逐行比对。结果——端口/models/依赖铁律 0 实质改动，watchlist.csv 去 score 列是阻塞2 的合理收敛且有需求侧承接。关闭。

## 建议栏（不立案，交设计职责）

- **（非阻塞瑕疵）§2 图 exporter 仍标「+3 文件，期2/3」**，而 §3 契约已是「新增 4 件」（多了 account_profile.csv）——数字未同步。契约节为准、交付面无歧义，故不阻塞；期3 方案或下次修蓝图时顺手改为「+4 文件」。
- **（期3 方案注意）v1 期3 交付的「互动降辅助」措辞在 v2 被删**，而需求目标 2 仍写「互动量降为辅助信号」。蓝图层打分公式未定属既定口径（「允许各期调，不算偏差」），不算缩水；但期3 方案若让 profile_score 含互动项，必须保证可从 account_profile.csv 导出列（或与 account_rank.csv 的 avg_interaction join）复算，否则「分项可复算（机械可验）」验收会破。
- **（留痕精度）需求验收回写除阻塞1 外还顺带写入了 account_profile.csv 载体（对应阻塞2）**，处置历史只标注了阻塞1——内容与蓝图一致、方向正确，不阻塞；后续回写建议逐项标注来源。

## 验证记录

- 读全文：开发蓝图 v2、开发蓝图 v1（对照）、蓝图评审1、需求 reports/专业账号追踪.md、评审规则、格式先例（v0 蓝图评审5）。
- **整文差量 diff**：`diff <(sed 's/v1/vN/g' 开发蓝图.md) <(sed 's/v2/vN/g' 开发蓝图v2.md)`——差异全部落在变更节声称范围（frontmatter/摘要/变更节/§2 三处标注/§3 两知情条+契约 4 处/§4 验收三行/§5 两风险），无其他改动。
- **回写亲核**：reports 第 41-42 行新验收口径（creator_notes 按原样 + topic_feed 窗内机械可验 + account_profile.csv 可复算）、第 50 行处置历史标注「响应蓝图评审 #1 阻塞1」。
- **外部事实抽核**（lab 根 `../MediaCrawler`）：`media_platform/xhs/core.py` grep `CRAWLER_MAX_NOTES_COUNT` 命中仅 133/134/141 三行，133-134 为 search() 内 <20 抬 20 的钳制，creator 路径无钳制；`media_platform/xhs/client.py:595-649` `get_all_notes_by_creator` 618 行 while 循环、641-645 行 remaining 截断，按主页顺序无时间排序——两处引用行号均精确。
- **本项目亲核**：`src/core/exporter.py:134-143` account_rank.csv 恰 7 列（account_id/nickname/relevant_note_count/keyword_hit_count/avg_interaction/account_score/note_ids），v2「旧 5 文件含 account_rank.csv 7 列不动」断言属实。
- **build_views 状态枚举核对**：`tools/build_views.py:36` PLAN_STATUS = {草拟中, 待评审, 需修改, 已通过, 已实施, 已归档}——「已评审」非法，通过后蓝图 v2 status 置「已通过」（最贴近枚举值）。
- `cd workbench && python3 tools/build_views.py`：见回执（EXIT=0）。
