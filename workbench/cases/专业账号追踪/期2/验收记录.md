---
title: 专业账号追踪 期2 验收记录
type: 验收记录
case: 专业账号追踪
phase: 2
created: 2026-07-03
---

## 验收结论

**通过**。期2「公开创作者内容同步与 watchlist 配置」交付：端口 `fetch_creator_notes` + MediaCrawler/fixture 双实现、manual（user_id/主页 URL 归一化，非法项 fail-fast）+ auto top-N 的 watchlist 合成（去重限量）、`watchlist.csv` + `creator_notes.csv` 两个新导出文件。无 `watchlist` 配置段 = 旧行为零变化，新能力全部 opt-in；MediaCrawler fork 零改动；`aggregate`/`account_ranker` 对起点零 diff（checklist no-touch 机械兜底）。

## 验收方式

**实测**（Gate 2 实测档，真实采集，用户当场授权）：`sample_mediacrawler.yaml` 配 manual 3 账号（路觅留学生课程辅导 / 辅无忧留学课业辅导规划站 / Alevel经济张老师-上海超级学长）真实跑通全管线（exit 0，导出 `data/exports/20260702T163155/`）——watchlist 合成 `manual 3 · auto 2/2 · total 5`，source 标注正确；`creator_notes.csv` 42 条，每账号条数 ≤ 10（10/10/10/10/2，账号发帖少不硬凑符合口径）、缺时间 0、每账号最新一条均落 2026-07-01~07-02 新近性良好；顺手项 `FetchResult.command` DEBUG 日志实测生效。此前 fixture demo、pytest **101 passed**（起点 77 + 新增 24）、ruff、预飞全绿均通过（代码评审独立复跑核实）。

## 过程要点

- 分工：Claude 出方案 + 组织子代理评审（sonnet 评审 / haiku 机械核对），**Codex 实施**（xhs-recon 仓 6 小步 commit `6e0c567..99a0eef` + docs `7342ea5`；MediaCrawler fork 零改动，单仓）。
- 方案评审两轮：#1 需修改（阻塞：部分失败时 `ok=False` 但 notes 非空的 pipeline 消费路径未钉死，v2 以端口规格 + pipeline 步骤 + 双侧单测三处联动关闭）+ 5 建议全吸收；#2 通过（差量评审，2 条表达级建议随实施落实）。
- 代码评审一轮**通过**（无阻塞，2 建议）：逐 commit 与方案 §4/§5 对账吻合、夹带扫描 0 命中、独立复跑预飞/测试全绿、实施偏差「无」经核实属实。
- Gate 1 拍板：非法 manual 项 **fail-fast**（报错退出，消息含原始 ref）。

## 已知项（非阻塞，留档）

1. **置顶帖占名额首次实测捕获**：「路觅」10 篇中含一条 2023-02-24 旧帖（置顶），符合蓝图预判，余量设计（每账号 10 篇）按预期兜住；是否做置顶识别留期3 实测干扰大再议（蓝图既定）。
2. **auto 账号未单独走 e2e fixture 路径**：e2e creator 夹具只覆盖 manual+过滤逻辑，auto 端到端由本次实测覆盖（代码评审建议 1，留档）。
3. creator 列表互动列为 0（=未采集），口径见方案 §3 非目标——互动信号复用 search 命中 notes，期3 打分沿此口径。

## 合入

`feat/pro-account-tracking-phase2` → `main`（`--no-ff`），推送 origin。

## 蓝图进度

期2/3 交付（creator 拉取与 watchlist）。期3（专业度打分与选题素材）随即开方案。`fetch_creator_notes` 契约与 `WatchAccount` 模型经实施验证够用，无需修蓝图（详见方案页蓝图验证备忘）。
