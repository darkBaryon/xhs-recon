---
title: 专业账号追踪 期3 方案 v2
type: 方案
case: 专业账号追踪
phase: 3
version: 2
status: 已实施
relates: [专业账号追踪]
created: 2026-07-03
---

## 给用户的摘要

期3 收官整个蓝图：给 watchlist 账号算**专业度分项**（垂直度 = creator 最新发帖命中领域关键词占比；活跃度 = 窗内发帖数），落 `account_profile.csv` 可复算；把窗内的 creator 笔记整理成**选题素材** `topic_feed.md`（人读）+ `topic_feed.jsonl`（下游选题项目直接消费）。旧文件（含期2 两件）逐字节口径不变，`report_input.md` 零改动。

- Before：期2 拉回 42 条 creator 笔记躺在 csv 里——哪个账号更专业、哪些帖新鲜可用，还得人眼扫。
- After：同一条命令 → 每账号垂直度/活跃度/综合分一张表 + 只含窗内新帖的选题素材包，出窗/缺时间的笔记不混入、计数留痕。

拍板清单：**无**（用量、窗口、导出契约均蓝图 Gate 已定；打分权重默认值属「允许各期调」精度口径，不构成取舍）。

## v1→v2 变更（供差量评审）

回应「方案评审 #1」（verdict 需修改，1 阻塞 + 3 建议；Q3-Q7 五条质疑评审已亲核关闭，含 profile_score 契约微扩判定成立）：

1. **关闭阻塞1（e2e pin 值歧义）**：§6 新增窗内断言用例已钉死——`_now_iso` pin 须为**合法 ISO**（如 `'2026-07-03T00:00:00+00:00'`），不可复用期2 双配置手法的 `'2026'` pin：`filter_notes(window_days>0, '2026')` 在 `time_window.py:32-33` 会 raise ValueError（`_parse_iso('2026')` 返回 None），测试将崩溃而非逻辑失败；先例照 `test_pipeline_window_filters_before_aggregate`（pin `'2026-07-02T00:00:00+00:00'` + window_days=30）。
2. **吸收建议1（paths set 断言更新）**：§5 步骤 5 补充——`export_all` 返回 dict 多三个 key，`test_pipeline_watchlist_fixture_exports_creator_files_and_keeps_old_outputs` 的 paths set 断言从 7 键更新到 10 键（`account_profile`/`topic_feed`/`topic_feed_jsonl`），旧 5 文件字节对比循环不受影响。
3. **吸收建议2（fixture 出窗行时间设计）**：§6 钉死——新增出窗分支夹具行选**明显早于窗口**的时间戳（2026-05-01 前后），任意合理 pin 值下出窗分支稳定触发，降低对 pin 选取的敏感性。
4. **吸收建议3（单测双覆盖）**：§6 `profile_accounts` 单测清单补「`window_days=30` + 部分笔记缺时间 → `recent_note_count` 只含窗内数」，与 e2e 窗内断言形成单测-集成双覆盖。

## 1. 起点

`起点 = c933f9e`（xhs-recon 仓 main，期2 Gate 2 验收合入 + 已推送 origin；src/ 干净，README.md 未提交漂移沿既定口径不动）。本期 MediaCrawler fork 零改动、adapters 零 diff——**期3 是纯 core/exporter/pipeline 计算与导出，不新增任何采集行为**。

## 2. 现状与约束盘点（写方案当时实测）

- `rank_accounts` 现状：`src/core/account_ranker.py:16-20` 签名，`DEFAULT_WEIGHTS`（`:5-9`），打分 `:32-34`，权重经 `run_research.py:139` 从 `ranking.weights` 传入——期3 分项打分照此权重合并模式。
- `AccountRank` 现有 7 字段（`src/models.py:38-45`）；`WatchAccount` 三字段（`:57-60`）；`Note` 的 `title/body/tags/published_at/source_keywords` 齐备（`:15-28`）。
- **creator 笔记的 `source_keywords` 为空**（期2 定案口径，实测 `data/exports/latest/creator_notes.csv` 确认）——垂直度命中判定必须基于 `title/body/tags` 对领域关键词的文本命中，不能复用 search 侧 `source_keywords`。
- 领域关键词全集现成：`expand_keywords`（`src/core/keyword_expander.py:7`，seed+同义词去重保序 `list[str]`），pipeline 已在 `run_research.py:113` 算好存于 `keywords` 变量。
- pipeline 期2 接线段：`watchlist` 变量 `run_research.py:142/154`，`creator_notes` 变量 `:163/177`，`window_days` `:136`，`collected_at` `:104`，`export_all` 调用 `:218-228`——期3 全部挂在 creator 拉取之后、export 之前，无需动既有步骤。
- **复用盘点**：窗过滤直接复用 `time_window.filter_notes`（`src/core/time_window.py:25-27`，输入 `list[Note]` 通用，`WindowFilterStats` 三计数 `:10-13` 恰好是验收口径要的对账数）；CSV 写出复用 `_write_csv`（`exporter.py:18-23`）；新文件条件写出照 `watchlist.csv` 先例（`exporter.py:174-178`，`None`=不写）；无现成 profile/feed 模块，`profile_accounts` 与 topic_feed 写出为新建，无重复造轮。
- e2e 挂点：`test_pipeline_watchlist_fixture_exports_creator_files_and_keeps_old_outputs`（`tests/test_pipeline_e2e.py:128`）已做「开 watchlist 双配置、旧文件字节对比」——期3 产物断言与「旧文件仍不变」都加在此挂点。
- 约束（铁律不变）：`core` 无 "xhs"；**`report_input.md` 零改动**（蓝图期3行「report_input 兼容不动」——出窗/缺时间计数落 `topic_feed.md` 头部与日志，不进报告）；旧 5 + 期2 两件导出契约逐字节口径不变（只新增 3 件）；`aggregate`/`parsers`/adapters/`time_window`/`note_selector`/`watchlist` 本期零 diff（机械兜底见 checklist）；无 `watchlist` 配置段 → 期3 逻辑整体跳过（opt-in 与期2 同门）。

## 3. 目标 / 非目标

**目标**：

1. `AccountRank` 新增分项字段（default 兼容旧调用，蓝图契约）+ `account_ranker.profile_accounts`：对 watchlist 账号计算 `vertical_ratio` / `recent_note_count` / `profile_score`；
2. 导出 `account_profile.csv`（列 `account_id,nickname,vertical_ratio,recent_note_count,profile_score`，蓝图逐字，可复算）；
3. 导出 `topic_feed.md` + `topic_feed.jsonl`：仅窗内 creator 笔记，出窗/缺时间不入 feed、在 feed 头部与日志计数（机械可验）；
4. pipeline 接线 + `ranking.weights` 新增 `vertical`/`activity` 两键（有内置默认，不配行为一致）。

**非目标**：不动 `report_input.md`（蓝图钉死）；不动采集面——adapters/fork/端口零改动，不追加任何拉取；不做置顶笔记识别（期2 实测已捕获现象，窗过滤天然滤掉 2023 旧置顶，暂无需额外机制）；不做中文分词/语义匹配（垂直度 v1 用文本包含命中，打分精度蓝图口径「允许各期调，不算偏差」）；不做选题生成（下游职责）；search 侧 `rank_accounts` 公式不动。

## 4. 接口与数据设计

```python
# src/models.py（只增不改；default 全零 = 旧构造/旧测试零改动）
AccountRank 新增：
    vertical_ratio: float = 0.0     # creator 最新 N 篇中命中领域关键词的占比（蓝图契约字段）
    recent_note_count: int = 0      # 时间窗内发帖数（蓝图契约字段）
    profile_score: float = 0.0      # 综合分（蓝图 account_profile.csv 既有列，落模型是承载所需的
                                    # 最小扩展——蓝图只列前两项，此为契约微扩，记入蓝图验证备忘）

# src/core/account_ranker.py（新增，不动 rank_accounts）
DEFAULT_PROFILE_WEIGHTS = {"vertical": 10.0, "activity": 1.0}
def profile_accounts(
    watchlist: list[WatchAccount], creator_notes: list[Note],
    domain_keywords: list[str], window_days: int, now_iso: str,
    weights: dict[str, float] | None = None,
) -> list[AccountRank]
# 每个 watchlist 账号一行（保持 watchlist 顺序），account_id/nickname 取自 WatchAccount，
# search 侧字段留默认 0（account_profile.csv 只导 5 列，不受影响）：
#   命中判定：note.title/body/tags 任一（casefold 后）包含任一 domain_keyword → 命中
#   vertical_ratio = 命中条数 / 该账号 creator 笔记数；0 条笔记 → 0.0
#   recent_note_count = filter_notes(该账号笔记, window_days, now_iso).kept
#     （复用 time_window；window_days<=0 = 不过滤 = 全部计入，与 search 段语义一致；
#      缺 published_at 按既定归 missing_time，不计入）
#   profile_score = w["vertical"] * vertical_ratio + w["activity"] * recent_note_count
# 纯函数、确定性、可测；core 无平台知识

# src/core/exporter.py（export_all 新增可选参数，None = 不写 = 旧行为；照 watchlist 先例）
export_all(..., account_profiles: list[AccountRank] | None = None,
                topic_feed: list[Note] | None = None,
                topic_feed_stats: WindowFilterStats | None = None,
                topic_feed_window_days: int = 0)
# account_profile.csv：account_id,nickname,vertical_ratio,recent_note_count,profile_score
#   （蓝图逐字 5 列；ratio 保留 4 位小数，score 保留 2 位——定死格式便于复算对账）
# topic_feed.jsonl：每行一条窗内笔记（json.dumps ensure_ascii=False）：
#   { account_id, nickname, source, note_id, title, body, tags: [...], url,
#     published_at, collected_at, like_count, collect_count, comment_count }
#   nickname/source 由 watchlist 参数（export_all 既有）按 account_id 映射；
#   互动三计数原样带出（creator 来源为 0=未采集，口径同期2，下游知情）
# topic_feed.md：头部一行机械对账统计
#   「窗口 N 天 · 入 feed K 条 · 出窗 X · 缺时间 Y · 账号 M」
#   随后按 watchlist 顺序分组：## nickname（account_id）→ 每条「- 日期 标题 url」
# 排序：账号间按 watchlist 序，账号内按 published_at 降序（确定性，e2e 可字节断言）

# src/pipelines/run_research.py（接线，creator 拉取之后、export_all 之前；
#                                watchlist 为 None 时整段跳过 = 旧行为零变化）
# 1. profiles = profile_accounts(watchlist, creator_notes, keywords,      # :113 的全集
#                                window_days, collected_at,
#                                config.get("ranking", {}).get("weights"))  # 与 rank_accounts 同源
# 2. feed_notes, feed_stats = filter_notes(creator_notes, window_days, collected_at)
#    日志一行：topic_feed kept/out_of_window/missing_time（对账口径）
# 3. export_all 追加传 account_profiles / topic_feed / topic_feed_stats / window_days

# config 契约（无新段；两份 sample 同步加两键，缺省=内置默认，行为一致）
ranking:
  weights: { note_count: 10, keyword_hit: 5, interaction: 0.01,
             vertical: 10.0, activity: 1.0 }    # 后两键期3新增，供 profile_score 复算
```

依赖方向不变：`pipelines → core → models`；profile 计算归 `account_ranker`（蓝图 §2 模块图「account_ranker(+专业度 v1，期3)」），窗过滤复用 `time_window`，无新层无新依赖。

## 5. 实现步骤（小步，每步可独立 CI 全绿 commit）

1. `models.AccountRank` 三新字段（default 全零）+ 旧构造回归断言（现有测试零改动即证兼容）；
2. `account_ranker.profile_accounts` + 单测（命中占比可复算 / 窗内计数 / 0 条账号 / 缺时间归 missing / `window_days=0` 全计入 / 权重覆盖与默认）；
3. `exporter` `account_profile.csv` + 测试（5 列蓝图逐字、格式 4 位/2 位小数、None 不写）；
4. `exporter` `topic_feed.md/jsonl` + 测试（jsonl schema 与 ensure_ascii、md 头部统计行、账号分组与排序确定性、None 不写）；
5. pipeline 接线 + 两份 sample 配置 + e2e（挂 `test_pipeline_e2e.py:128` 断言三新文件存在与「旧文件仍不变」，其 paths set 断言从 7 键更新到 10 键；新增用例断言 **jsonl 全部 `published_at` 落窗内 + `kept+out+missing == creator 总数`**——验收口径的机械形态）。

## 6. 测试策略

- 单测（行为断言优先）：
  - `profile_accounts`：3 篇中 2 篇命中 → `vertical_ratio==0.5` 类可手算断言；窗内 2 篇 → `recent_note_count==2`；**`window_days=30` + 部分笔记缺时间 → `recent_note_count` 只含窗内数**（与 e2e 窗内断言双覆盖，评审 #1 建议3）；`profile_score == 10*ratio + 1*count` 复算；0 条 creator 笔记账号三值全 0 且仍出行；命中不区分大小写；tags 命中与 body 命中等价；
  - `exporter`：不传新参目录**不出现**三新文件（旧行为回归）；`account_profile.csv` 列头逐字；jsonl 逐行 `json.loads` 可解析且键齐；md 首行统计与 stats 一致；
  - pipeline：无 `watchlist` 段时 `profile_accounts` 不被调用（mock 断言）、三新文件不产出。
- e2e（挂点 `tests/test_pipeline_e2e.py:128` 既有双配置手法，paths set 断言 7→10 键）：开 watchlist 跑 fixture → 三新文件存在；**旧 5 + 期2 两件与期2 版本产出逐字节一致**（`report_input.md` 零改动的机械证明）；新增用例：jsonl 全部窗内 + 三计数对账——**该用例 `window_days>0`，`_now_iso` pin 必须用合法 ISO（如 `'2026-07-03T00:00:00+00:00'`），不可复用期2 的 `'2026'` pin**（`filter_notes(window_days>0, '2026')` 在 `time_window.py:32-33` raise ValueError，测试会崩溃而非逻辑失败；先例照 `test_pipeline_window_filters_before_aggregate`）。creator fixture 出窗分支：给夹具**新增**时间明显早于窗口的行（2026-05-01 前后），只增不改既有 4 行（避免破坏期2 断言），任意合理 pin 值下出窗分支稳定触发。
- 手动验收（Gate 2）：真实数据不必重采——直接对期2 实测产物复算：取 `data/exports/20260702T163155/creator_notes.csv` 任一账号，人工数命中/窗内条数，对照本期重跑（fixture 或真实均可）的 `account_profile.csv` 分项可复算；`topic_feed` 不含「路觅」那条 2023 置顶旧帖（窗过滤兜底的实证）。
- 盲区：垂直度文本包含命中对中文表述变体（如「留学生辅导」vs「留学辅导」子串关系）偏宽松，属 v1 精度口径（蓝图「允许各期调，不算偏差」），下游用分项列可自行重权。

## 7. 验收标准 + 检查清单 YAML

机械核验以同目录 [checklist.yaml](checklist.yaml) 为唯一事实源：lint/format/pytest/fixture-demo 全绿；`vertical_ratio`/`profile_accounts`/`account_profile.csv`/`topic_feed` 符号落地；demo 产物含三新文件与旧 7 件；core 无 "xhs"、src 无 `print(`；`aggregate`/`parsers`/三个 adapter 文件/`time_window`/`note_selector`/`watchlist` 对起点 `c933f9e` 零 diff（本期只动 models/account_ranker/exporter/run_research 四个 src 文件，白名单反向兜底）。人话：不配 watchlist 段 → 一切旧行为（含期2）不变；配了 → 多出三件可复算、可对账的产物，旧文件一字节不动。

## 8. 风险与禁止事项

- **时间序蹭词水帖混入 feed**：窗内笔记未做互动/语义过滤，水帖会进 feed——蓝图既定风险，靠 `account_profile` 分项供下游按账号加权自滤；本期不加过滤规则（多做即偏差）。
- **fixture 时间分布与 pin 值耦合**：e2e 窗内断言依赖 creator fixture 的 `published_at` 与 pin 的 `collected_at` 相对关系——§6 已定口径：只增不改既有夹具行，新增行时间错开覆盖出窗/缺时间分支。
- **profile_score 落模型属契约微扩**：蓝图 §3 只列两分项字段，综合分列却在 csv 契约中——本方案取「模型承载」解法（最小、可测），实施后记入蓝图验证备忘，由评审判定是否需修蓝图（预判：不需，csv 契约本身已含该列）。
- 禁止：动 `report_input.md` 任何字节口径；动 adapters/端口/fork；动 `rank_accounts` 公式与旧导出 7 件列；加过滤规则、分词依赖、新配置段；任何采集行为变化。

## 9. 回滚口径

未合主线前弃功能分支（建议 `feat/pro-account-tracking-phase3`）即可；纯计算与导出扩展，无 fork 改动、无数据迁移、无采集行为变化，回滚零残留。已 commit 的去留由用户裁决。

---

## 实施记录

- 实施分支：`xhs-recon` 仓 `feat/pro-account-tracking-phase3`，从 `c933f9e` 创建；实施期间工作树原有 `README.md` 未提交漂移未纳入本期改动。
- MediaCrawler fork 与 adapters：本期零改动；采集行为、端口、fork 均未改。
- 步骤 1：`b58fb0c step 1: add account profile rank fields`，`AccountRank` 新增 `vertical_ratio/recent_note_count/profile_score` 三个默认零值字段，旧构造与旧测试不需改动。
- 步骤 2：`177fd92 step 2: add account profile scoring`，新增 `profile_accounts`，按 watchlist 顺序输出 profile 行，命中判定覆盖 title/body/tags casefold 包含，窗内计数复用 `filter_notes`，权重复用 `ranking.weights` 的 `vertical/activity`。
- 步骤 3：`f62eb10 step 3: export account profiles`，新增可选 `account_profile.csv` 导出，列头逐字 `account_id,nickname,vertical_ratio,recent_note_count,profile_score`，ratio 4 位、score 2 位。
- 步骤 4：`8eba121 step 4: export topic feed files`，新增可选 `topic_feed.md` / `topic_feed.jsonl`，jsonl 使用 `ensure_ascii=False`，Markdown 头部写窗口/入 feed/出窗/缺时间/账号数，排序按 watchlist 序 + `published_at` 降序。
- 步骤 5：`3c699c8 step 5: wire profile and topic feed pipeline`，pipeline 接入 profile/feed，sample 权重补 `vertical/activity`，e2e paths set 7→10，新增合法 ISO pin 的窗内断言；creator fixture 仅追加新账号 2026-05-01 出窗行，既有 4 行未改。
- 验证：`uv run ruff check .`、`uv run ruff format --check .`、`uv run pytest -q` 均已通过（106 passed）；最终预飞命令 `python3 workbench/tools/preflight.py --config workbench/cases/专业账号追踪/期3/checklist.yaml --repo .` 全绿。

## 实施偏差记录

无。测试、sample 配置、creator fixture 与本页实施记录按 §5/§6/流程要求同步更新；业务代码仍只触及 `src/models.py`、`src/core/account_ranker.py`、`src/core/exporter.py`、`src/pipelines/run_research.py` 四个目标文件。`report_input.md` 口径、adapters、端口、fork、`rank_accounts` 公式、旧 7 件导出列均未改。

## 蓝图验证备忘

- `profile_score` 契约微扩已按评审口径落地：`AccountRank` 三字段默认零值，旧构造安全；`account_profile.csv` 固定 5 列承载综合分，`account_rank.csv` 仍按旧 7 列显式写出，不会被模型扩字段带偏。无需修蓝图出 v3。
- `topic_feed` 窗过滤口径成立：pipeline 对 `creator_notes` 复用 `filter_notes`，jsonl/md 只接收 kept 笔记，出窗/缺时间只进入 Markdown 头部和日志；新增 e2e 用合法 ISO pin `2026-07-03T00:00:00+00:00`，断言 jsonl 全部窗内且 `kept+out+missing == creator 总数`。无需修蓝图出 v3。
- 期3 收官未引入新配置段、分词依赖或采集行为变化；`vertical/activity` 仅作为 `ranking.weights` 同源新增键并有内置默认。蓝图契约够用，无需出 v3。

## Gate 决策结果

- **2026-07-03 Gate 1 放行**（用户「通过」，方案评审 #2 通过后）：无拍板项（权重默认值属蓝图既定精度口径；`profile_score` 契约微扩经评审判定成立）。实施移交 Codex，按 §5 五步小步 commit，预飞全绿后交代码评审。评审 #2 建议 B-1（e2e 出窗夹具行挂**新账号**——挂到既有 e2e 已断言的账号会破坏 `creator_notes.csv` 精确断言）随实施落实，不另出 v3。来源：用户当场明说。
