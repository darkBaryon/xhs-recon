---
title: 专业账号追踪 期3 代码评审 #1
type: 代码评审
case: 专业账号追踪
phase: 3
round: 1
target: 期3/方案v2 实施
verdict: 通过
created: 2026-07-03
---

## 结论

**通过**（无阻塞项；2 条非阻塞建议）。覆盖 `c933f9e..HEAD` 共 6 commit（5 实施步骤 + 1 文档落档 85fafaa，docs commit 只动 workbench），实现与方案 v2 §4 接口设计、§5 五步逐条吻合。接口签名、字段口径、格式精度、窗过滤复用、排序确定性、路径集合断言（7→10 键）、B-1（出窗夹具行挂新账号 603d）全部亲核通过。夹带扫描：src 仅 4 个白名单文件有改动，no-touch 路径零 diff，两份 sample yaml 只加 `vertical/activity` 两键，`_write_report` 段零 diff（`report_input.md` 机械证明），旧文件字节对比通过（11 e2e 全绿）。

---

## 亲跑记录

| 命令 | 结果 |
|---|---|
| `uv run ruff check .` | All checks passed!（exit 0）|
| `uv run ruff format --check .` | 37 files already formatted（exit 0）|
| `uv run pytest -q` | **106 passed** in 0.33s（起点 `c933f9e` 时为 101 passed，本期新增 5 commit 增加约 ~5 用例）|
| `python3 workbench/tools/preflight.py --config workbench/cases/专业账号追踪/期3/checklist.yaml --repo .` | **全绿**（命令 5/5；产物 10/10；符号 6/6；禁留 0 命中）|
| `git diff --quiet c933f9e -- src/core/aggregator.py src/adapters src/core/time_window.py src/core/note_selector.py src/core/watchlist.py src/core/ports.py` | exit 0（no-touch 亲跑非仅引用预飞）|
| `pytest tests/test_pipeline_e2e.py -v -q` | 11 passed（期2 已有 + 本期新增 2 个 e2e 用例，旧断言未破坏）|

---

## 逐 commit 对账表

### commit `b58fb0c` — step 1: add account profile rank fields（方案 §5 步骤1）

方案要求：`AccountRank` 新增 `vertical_ratio: float = 0.0` / `recent_note_count: int = 0` / `profile_score: float = 0.0`，default 全零，旧构造/旧测试零改动。

亲核 diff：
- `src/models.py` 第 46-48 行新增三字段，字段名、类型、默认值与 §4 逐字一致。✅
- 追加在 `note_ids: list[str]` 之后（不改旧字段顺序），旧构造仍然合法（pydantic 有默认值）。✅
- 改动仅 1 文件（models.py），无夹带。✅

与方案无偏差。

---

### commit `177fd92` — step 2: add account profile scoring（方案 §5 步骤2）

方案要求：`DEFAULT_PROFILE_WEIGHTS = {"vertical": 10.0, "activity": 1.0}`；`profile_accounts(watchlist, creator_notes, domain_keywords, window_days, now_iso, weights=None) -> list[AccountRank]`；命中判定 title/body/tags casefold 包含；`vertical_ratio = 命中/总数`（0条→0.0）；`recent_note_count = filter_notes(...).kept`；`profile_score = w["vertical"]*ratio + w["activity"]*count`；watchlist 顺序；单测 7 组。

亲核 diff：
- `_matches_domain`：`" ".join([note.title, note.body, *note.tags]).casefold()` 拼接后统一 casefold，`any(keyword.casefold() in haystack ...)` 关键词也 casefold。title/body/tags 三路等价命中。✅
- `w = {**DEFAULT_PROFILE_WEIGHTS, **(weights or {})}` 合并逻辑：当 `ranking_weights` 含 `note_count/keyword_hit/interaction` 但不含 `vertical/activity` 时，合并后 `vertical/activity` 保持默认；当配置新增 `vertical/activity` 时，覆盖默认。经 Python 亲算验证（见「可证伪质疑 Q1」）。✅
- `recent_notes, _stats = filter_notes(account_notes, window_days, now_iso); recent_count = len(recent_notes)`——复用 `time_window.filter_notes` 返回的 kept 列表，与方案 §4「`.kept`」口径一致（注：`filter_notes` 返回 `(list[Note], WindowFilterStats)`，`len(recent_notes)` 等价于 `stats.kept`）。✅
- 单测 `test_profile_accounts_scores_vertical_ratio_and_recent_count`（`window_days=30`，合法 ISO pin）：
  - A1 3 条笔记（N1 命中 title、N2 命中 body、N3 不命中 tags，N3 `2026-05-01` 距 now 63天>30天出窗）→ `vertical_ratio=2/3`，`recent_note_count=2`，`profile_score=10*(2/3)+2`。✅
  - A2 1 条 N4（tags=[FINAL自救] casefold 命中，`published_at=""` 缺时间归 missing）→ `vertical_ratio=1.0`，`recent_note_count=0`。✅（缺时间不进 recent 的验证）
  - A3 0 条笔记 → 三值全 0 且仍出行。✅
- 单测 `test_profile_accounts_window_zero_keeps_all_and_weights_are_configurable`（`window_days=0`，2 条含 1 条缺时间）→ `recent_note_count=2`（`window_days=0` 全计入，缺时间也计），权重覆盖 `{vertical:20, activity:2}`，`profile_score=20*0.5+2*2=14`。✅
- 命中不区分大小写（`essay辅导` vs `Essay辅导`，`final自救` vs `FINAL自救`）均经手算可追溯。✅
- 改动仅 2 文件（account_ranker.py/test_account_ranker.py），无夹带。✅

与方案无偏差。

---

### commit `f62eb10` — step 3: export account profiles（方案 §5 步骤3）

方案要求：`export_all` 新增可选 `account_profiles: list[AccountRank] | None = None`；`account_profile.csv` 列头逐字五列；`vertical_ratio` 保留 4 位小数，`profile_score` 保留 2 位；None 不写。

亲核 diff：
- `export_all` 签名追加 `account_profiles: list[AccountRank] | None = None`（照 watchlist 先例，None=不写）。✅
- 列头 `["account_id", "nickname", "vertical_ratio", "recent_note_count", "profile_score"]` 与 §4 逐字一致。✅
- 格式：`f"{r.vertical_ratio:.4f}"` / `r.recent_note_count`（int 原样）/ `f"{r.profile_score:.2f}"`——4 位/2 位符合。✅
- 单测 `test_export_all_writes_account_profile_when_passed`：输入 `vertical_ratio=2/3, profile_score=18.666`，断言行 `["U1","作者甲","0.6667","12","18.67"]`——`2/3=0.6666...` 4 位截断正确，`18.666` 2 位四舍五入正确。✅
- `test_export_all_writes_five_files` 追加三条反断言（`account_profile not in paths`，`.csv` 不存在）。✅
- 改动仅 2 文件（exporter.py/test_exporter.py），无夹带。✅

与方案无偏差。

---

### commit `8eba121` — step 4: export topic feed files（方案 §5 步骤4）

方案要求：`export_all` 新增 `topic_feed / topic_feed_stats / topic_feed_window_days` 三参；`topic_feed.jsonl`（`ensure_ascii=False`，13 键 schema，nickname/source 由 watchlist 映射）；`topic_feed.md`（头部统计行 + `## nickname（account_id）` 分组 + `- 日期 标题 url`）；账号间 watchlist 序/账号内 published_at 降序；None 不写。

亲核 diff：
- `_write_topic_feed_jsonl` 中 `json.dumps(..., ensure_ascii=False)` ✅
- jsonl 13 键：`account_id/nickname/source/note_id/title/body/tags/url/published_at/collected_at/like_count/collect_count/comment_count`，与 §4 逐字一致。✅
- `_ordered_topic_notes`：账号间按 `order.get(account_id, len(order))` 排（watchlist 序，未在 watchlist 的排末）；账号内 `sorted(..., key=lambda note: (note.published_at, note.note_id), reverse=True)` 降序。✅
- md 头部格式：`"窗口 {window_days} 天 · 入 feed {stats.kept} 条 · 出窗 {stats.out_of_window} · 缺时间 {stats.missing_time} · 账号 {account_count}"`——`account_count = len({note.account_id for note in ordered_notes})`（feed 内实际账号数，非 watchlist 总数；见「可证伪质疑 Q2」）。✅
- `topic_feed_stats=None` 时 fallback `WindowFilterStats(kept=len(topic_feed), out_of_window=0, missing_time=0)`——防御性设计，`export_all` 在 `topic_feed is not None` 分支内已保证有 stats（pipeline 总传 stats），此 fallback 不影响正确路径。✅
- 单测 `test_export_all_writes_topic_feed_when_passed`：
  - jsonl ensure_ascii 验证：`"\\u4f5c" not in jsonl_text` 检测中文未被转义。✅
  - 键集合断言 13 键齐全。✅
  - 排序：N-u2（U2账号，watchlist[0]）在前 > N-new（U1，`2026-07-03`）> N-old（U1，`2026-07-01`）→ 账号间 watchlist 序，账号内时间降序。✅
  - md 头部 `"窗口 30 天 · 入 feed 3 条 · 出窗 1 · 缺时间 1 · 账号 2"` 逐字断言。✅
- 改动仅 2 文件（exporter.py/test_exporter.py），无夹带。✅

与方案无偏差。

---

### commit `3c699c8` — step 5: wire profile and topic feed pipeline（方案 §5 步骤5）

方案要求：pipeline 接线（`watchlist_cfg is None` 整段跳过）；sample 两份 +`vertical/activity` 两键；e2e `paths set 7→10` 键；新增窗内断言用例（合法 ISO pin）；creator fixture 只增不改（新增 603d 出窗行）。

亲核 diff：

**pipeline 接线**（逐步核对）：
1. `window_days = search_cfg.get("window_days", 0)` 提前提取，复用于 profile 和 feed。✅
2. `ranking_weights = config.get("ranking", {}).get("weights")` 提前提取，与 `rank_accounts` 同源传入 `profile_accounts`。✅
3. `account_profiles/topic_feed_notes/topic_feed_stats` 初始化 `None`，`if watchlist_cfg is not None:` 块内才赋值。✅
4. `profile_accounts(watchlist, creator_notes, keywords, window_days, collected_at, ranking_weights)` 签名与 §4 完全一致；`keywords` 为 `expand_keywords` 全集（`:113` 的变量）。✅
5. `topic_feed_notes, topic_feed_stats = filter_notes(creator_notes, window_days, collected_at)` 复用 `time_window`，日志三计数对账。✅
6. `export_all` 追加四参 `account_profiles / topic_feed / topic_feed_stats / topic_feed_window_days`。✅

**no-watchlist 跳过**：`watchlist_cfg=None` → `account_profiles=None / topic_feed_notes=None` → `export_all` 不写三新文件。e2e `test_pipeline_without_watchlist_does_not_call_fetch_creator_notes` 追加三条 `not in paths` 断言。✅

**paths set 7→10 键**：`assert set(watch_paths) == {"accounts","notes","account_rank","typical_notes","report_input","watchlist","creator_notes","account_profile","topic_feed","topic_feed_jsonl"}` 共 10 键。✅

**旧文件字节对比**：`for key in ["accounts","notes","account_rank","typical_notes","report_input"]: assert read_bytes(watch)==read_bytes(base)` 保持 5 件，未缩减。`_write_report` 函数 diff 亲核（git diff 中 `-` 行为零，函数体未改）——`report_input.md` 零改动机械证明。✅

**新增 e2e 窗内断言用例** `test_pipeline_topic_feed_keeps_only_window_notes_and_counts_stats`：
- pin `"2026-07-03T00:00:00+00:00"` 合法 ISO（非 `'2026'`），`window_days=30`。✅
- watchlist 含 `601d`（2 条窗内）和 `603d`（1 条出窗，`2026-05-01`，距 now 63天）。✅
- jsonl 断言仅 2 条（601d 的两条），`age_days <= 30` 逐条断言。✅
- md 头部 `"窗口 30 天 · 入 feed 2 条 · 出窗 1 · 缺时间 0 · 账号 1"` 逐字断言。✅
- `kept+out+missing == len(creator_rows)` 三计数对账（2+1+0=3）。✅
- `profile_rows` 两账号逐行断言（`603d recent_note_count=0` 验证出窗账号三值不全零但 recent=0）。✅

**B-1 落地核查**：fixture 新增行 `user_id=603d0481000000000101cc48`，为新账号（期2 e2e watchlist 含 601d manual + search fixture auto top2（62f2529b 等），不含 603d）→ 期2 e2e `creator_rows` 断言 `["6a4661cd","6a4661a0"]` 不受影响。亲跑 `pytest tests/test_pipeline_e2e.py -v -q` 11 passed 确认。✅

**fixture 只增不改**：git diff 显示 `tests/fixtures/creator_contents_sample.jsonl` 只有一行 `+`（新增 603d 行），旧 4 行未改。✅

**两份 sample yaml**：均只在 `ranking.weights` 下新增 `vertical: 10.0` / `activity: 1.0` 两键，无新段、无其他改动。✅

改动文件：`configs/sample.yaml / configs/sample_mediacrawler.yaml / src/pipelines/run_research.py / tests/fixtures/creator_contents_sample.jsonl / tests/test_pipeline_e2e.py`，共 5 文件，均在方案范围内。✅

与方案无偏差。

---

### commit `85fafaa` — docs: record phase3 implementation（docs only）

`git show --stat 85fafaa` 确认：5 文件全在 `workbench/cases/专业账号追踪/期3/` 下（checklist.yaml / 方案v1.md / 方案v2.md / 方案评审1.md / 方案评审2.md），src/tests/configs 零 diff。✅

---

## 关键口径落地核查

| 口径 | 核查项 | 结论 |
|---|---|---|
| `account_profile.csv` 五列蓝图逐字 | 列头 `["account_id","nickname","vertical_ratio","recent_note_count","profile_score"]` diff 亲读，与 §4 逐字一致 | ✅ |
| `vertical_ratio` 4 位小数 / `profile_score` 2 位小数 | `f"{r.vertical_ratio:.4f}"` / `f"{r.profile_score:.2f}"`；单测断言 `"0.6667"/"18.67"` | ✅ |
| `profile_accounts` 命中判定 casefold 包含 | `_matches_domain`：拼接 title/body/tags casefold，关键词也 casefold；`FINAL自救` vs `final自救` 单测验证 | ✅ |
| 0 条账号三值全 0 仍出行 | A3 无 creator 笔记 → `profiles.append(AccountRank(..., vertical_ratio=0.0, recent_note_count=0, profile_score=0.0))`；单测断言 | ✅ |
| `recent_note_count` 复用 `filter_notes` | `recent_notes, _stats = filter_notes(account_notes, window_days, now_iso); recent_count = len(recent_notes)` | ✅ |
| `profile_score` 公式与权重同源 `ranking.weights` | `ranking_weights = config.get("ranking",{}).get("weights")`，同传 `rank_accounts` 与 `profile_accounts`；`w = {**DEFAULT_PROFILE_WEIGHTS, **(weights or {})}` | ✅ |
| `topic_feed` 仅窗内 | pipeline `filter_notes(creator_notes, window_days, collected_at)` 返回 kept，only kept 传 `topic_feed=`；e2e 逐条 `age_days<=30` 断言 | ✅ |
| md 头部统计行格式 | `"窗口 {N} 天 · 入 feed {K} 条 · 出窗 {X} · 缺时间 {Y} · 账号 {M}"` 逐字，两个单测+e2e 断言 | ✅ |
| 账号间 watchlist 序/账号内 published_at 降序 | `_ordered_topic_notes` 亲读；单测排序断言 N-u2 < N-new < N-old | ✅ |
| jsonl `ensure_ascii=False` | `json.dumps(..., ensure_ascii=False)`；单测 `"\\u4f5c" not in text` | ✅ |
| topic_feed 计数不进 `report_input.md` | `_write_report` 函数 diff 零 `-` 行（函数体未改）；e2e 旧 5 文件字节对比通过 | ✅ |
| 无 watchlist 段期3 逻辑整体跳过 | `watchlist_cfg is None` → 三变量保持 None → export_all 不写三新文件；e2e `not in paths` + `_NoCreatorCallAdapter` mock 验证 | ✅ |
| e2e pin 合法 ISO（非 `'2026'`） | 新增用例 pin `"2026-07-03T00:00:00+00:00"`；旧 watchlist e2e pin `'2026'` 但 `window_days` 未设（默认 0），`filter_notes(window_days=0)` 不进 ValueError 路径 | ✅ |
| 出窗夹具行挂新账号（B-1）且只增不改既有 4 行 | fixture diff 只有一行 `+`，user_id=603d 为新账号，期2 e2e watchlist 不含 603d，亲跑 11 passed 机械证明 | ✅ |
| paths set 7→10 键 | e2e 断言从旧 7 键到新 10 键（+`account_profile/topic_feed/topic_feed_jsonl`）亲读 | ✅ |
| src 白名单四文件 / no-touch 零 diff | `git diff c933f9e..HEAD --stat` 中 src 仅 `models.py/account_ranker.py/exporter.py/run_research.py`；no-touch 亲跑 exit 0 | ✅ |

---

## §6 测试覆盖核对

| §6 要求测试 | 落地情况 |
|---|---|
| `profile_accounts`：3篇中2篇命中 → `vertical_ratio==0.5` 手算断言 | `test_profile_accounts_scores_vertical_ratio_and_recent_count`：A1 3条2命中 `2/3`，A2 1条1命中 `1.0` ✅ |
| `profile_accounts`：窗内2篇 → `recent_note_count==2` | A1 N3 出窗 → `recent=2` ✅ |
| `profile_accounts`：`window_days=30` + 部分笔记缺时间 → `recent_note_count` 只含窗内数 | A2 N4 `published_at=""` → `missing_time=1`，`recent=0`（单条全缺时间代替部分缺时间，行为等价，见「建议1」）✅/⚠️ |
| `profile_score == 10*ratio + 1*count` 复算 | A1 `10*(2/3)+2`，A2 `10*1.0+0=10.0`，A3 `0.0` 均手算断言 ✅ |
| 0条 creator 笔记账号三值全0仍出行 | A3 出行且三值为0 ✅ |
| 命中不区分大小写 | `essay辅导` vs `Essay辅导`，`final自救` vs `FINAL自救` ✅ |
| tags 命中与 body 命中等价 | N4 tags 命中、N2 body 命中均断言 ✅ |
| `window_days=0` 全计入 | `test_profile_accounts_window_zero_keeps_all_and_weights_are_configurable`：2条含1条缺时间 `recent=2` ✅ |
| 权重覆盖与默认 | `{vertical:20,activity:2}` 覆盖默认，`profile_score=14` ✅ |
| exporter：不传新参不出三新文件 | `test_export_all_writes_five_files` 追加 5 条反断言 ✅ |
| exporter：`account_profile.csv` 列头逐字 | 单测 `["account_id","nickname","vertical_ratio","recent_note_count","profile_score"]` ✅ |
| exporter：jsonl 逐行可解析且键齐 | 13 键 `set` 断言 ✅ |
| exporter：md 首行统计与 stats 一致 | `"窗口 30 天 · 入 feed 3 条 · 出窗 1 · 缺时间 1 · 账号 2"` 逐字断言 ✅ |
| pipeline：无 watchlist 段时三新文件不产出 | `test_pipeline_without_watchlist`：`account_profile/topic_feed/topic_feed_jsonl not in paths` ✅ |
| e2e：paths set 7→10 键 | `assert set(watch_paths) == {...10个键...}` ✅ |
| e2e：旧5+期2两件字节对比 | `for key in [5件]: read_bytes(watch)==read_bytes(base)` ✅ |
| e2e：新增用例 jsonl 全部窗内 + 三计数对账 | `test_pipeline_topic_feed_keeps_only_window_notes_and_counts_stats`：`age_days<=30` 逐条 + `kept+out+missing==total` ✅ |

---

## 夹带扫描

- `git diff c933f9e..HEAD --stat` 全量：15 文件，src 侧仅 `src/models.py / src/core/account_ranker.py / src/core/exporter.py / src/pipelines/run_research.py` 四个白名单文件。✅
- no-touch 路径（`aggregator.py / src/adapters / time_window.py / note_selector.py / watchlist.py / ports.py`）：亲跑 `git diff --quiet c933f9e -- ...` exit 0，非仅预飞引用。✅
- `src/core/exporter.py` 旧导出列：diff 中零 `-` 行（除 import 行），全为 `+`。`_write_report` 函数体未改（只出现在 `@@ ... @@` 上下文行，非修改行）。✅
- 两份 sample yaml：各自只加 `vertical: 10.0 / activity: 1.0` 两键，在 `ranking.weights` 下，无新顶级段。✅
- `tests/fixtures/creator_contents_sample.jsonl`：仅追加 1 行（603d 账号），旧 4 行原样，diff 只有一行 `+`。✅
- `uv.lock / pyproject.toml`：`git diff c933f9e..HEAD --stat` 未列出（零 diff），无新第三方依赖。✅
- `src/core` 无 `xhs` 命中（预飞禁留 0）；`src` 无 `print(` 命中（预飞禁留 0）。✅

---

## 可证伪质疑清单

### 质疑 Q1【已关闭】：`w = {**DEFAULT_PROFILE_WEIGHTS, **(weights or {})}` 在 `ranking_weights` 不含 `vertical/activity` 时是否仍能用 default？

**可证伪点**：若合并逻辑把 `ranking_weights` 的 `note_count/keyword_hit/interaction` 三键覆盖掉 `DEFAULT_PROFILE_WEIGHTS`，则 `vertical/activity` 会消失，`w["vertical"]` 抛 KeyError。

**验证**：Python dict 解包 `{**A, **B}` 中 A 先铺 B 后覆盖——`B = ranking_weights` 若不含 `vertical/activity`，则 A 的 `vertical=10.0/activity=1.0` 保留。亲算 `{**{"vertical":10.0,"activity":1.0}, **{"note_count":10,"keyword_hit":5,"interaction":0.01}}` → merged 含 `vertical=10.0/activity=1.0`。sample.yaml 新增后传入则覆盖为配置值。**关闭**。

---

### 质疑 Q2【已关闭】：md 头部「账号 M」计的是 feed 内实际账号数还是 watchlist 总账号数？§4 口径是否一致？

**可证伪点**：若 M 是 watchlist 总数（2个账号含 603d），e2e 断言 `账号 1` 会失败；若 M 是 feed 内去重账号数（603d 全出窗未进 feed），则 M=1 与断言一致。

**验证**：`_write_topic_feed_md` 代码 `account_count = len({note.account_id for note in ordered_notes})`——`ordered_notes` 是 `topic_feed`（kept 列表），603d 笔记出窗不进 feed → 集合中无 603d → account_count=1。e2e 断言 `"账号 1"` 通过。§4 原文「账号 M」语义上指 feed 内有多少账号，实现符合直觉。**关闭**。

---

### 质疑 Q3【已关闭】：`profile_accounts` 在 `watchlist=[]`（空列表，非 None）时的行为是否与 `None` 一致？

**可证伪点**：若 `watchlist=[]` 时 `profile_accounts` 被调用（因 `if watchlist_cfg is not None:` 已进入，再无 `if watchlist:` 保护），而 `account_profiles=[]` 传入 `export_all`，则 `account_profile.csv` 会写出只含表头的文件——而方案期望「无 watchlist 段→不写」。

**验证**：pipeline 控制流：`watchlist_cfg is not None` 进入后，`if watchlist:` 保护的是 `fetch_creator_notes`，但 `profile_accounts` 在 `if watchlist_cfg is not None:` 块末无条件调用。`watchlist=[]` → `profile_accounts([], ...) = []` → `export_all(account_profiles=[])` → `if account_profiles is not None:` 写空表头。此行为与期2 `creator_notes=[]` 写空表头一致（期2 亦在 `except NotImplementedError` 分支写空 creator_notes.csv）。方案 §3 说的「无 `watchlist` 配置段 → 期3 逻辑整体跳过」是指 `watchlist_cfg=None` 而非 `watchlist=[]`；空 watchlist 列表写空表头是合理退化，不算偏差。**关闭**。

---

### 质疑 Q4【已关闭】：期2 e2e `test_pipeline_watchlist_fixture_exports_creator_files_and_keeps_old_outputs` 断言 `creator_rows` 为 `["6a4661cd","6a4661a0"]`——新增 603d 夹具行是否破坏此断言？

**可证伪点**：若期2 e2e 的 watchlist 包含 603d 账号，则 fixture 新增行会被 `fetch_creator_notes` 读取，`creator_rows` 变为 3 条，断言失败。

**验证**：期2 e2e watchlist `auto_top_n=2, manual=[601d URL]`——search fixture 的 top2 auto 为 `62f2529b` 等账号（不含 603d），manual 为 601d，watchlist 共 3 个账号均不是 603d。`fetch_creator_notes([601d, auto1, auto2], ...)` 过滤 creator fixture 时 603d 账号不在 `account_ids` 列表内 → 不读入。亲跑 `pytest tests/test_pipeline_e2e.py` 11 passed 机械证明。**关闭**。

---

### 质疑 Q5【已关闭】：`test_pipeline_without_watchlist_does_not_call_fetch_creator_notes` 用 `paths not in` 代替 `profile_accounts` mock 断言——是否严格满足 §6「mock 断言」要求？

**可证伪点**：`paths not in` 能证明「三新文件未产出」，但不能直接证明 `profile_accounts` 函数未被调用（理论上存在「被调用但结果被丢弃」的路径）。若 §6 要求必须是函数调用级的 mock 断言，则此测试精度不足。

**验证**：亲读 `run_research.py` 控制流——`profile_accounts` 调用在 `if watchlist_cfg is not None:` 块内（:187 行），`watchlist_cfg=None` 时代码路径不经过此调用。`paths not in` 断言通过 → `export_all` 未收到 `account_profiles` 非 None 值 → `profile_accounts` 未被调用（逻辑等价）。形式上 `not in paths` 是行为断言而非 mock 断言，测试目标已覆盖，安全。**关闭**（形式轻微偏差，记为「建议2」非阻塞）。

---

## 实施偏差记录对账

方案实施偏差记录：「无」。

亲核：
- §3 目标 4 条全部落地（AccountRank 三新字段 / profile_accounts + account_profile.csv / topic_feed.md+jsonl / pipeline 接线 + 配置同步）。✅
- §5 五步每步均有对应 commit（b58fb0c/177fd92/f62eb10/8eba121/3c699c8），commit message 与步骤描述对应。✅
- no-touch 文件零 diff（亲跑确认）。✅
- 旧 7 件导出列不变（`_write_report` 零改动，exporter diff 无 `-` 行）。✅
- B-1（出窗夹具行挂新账号）按 Gate 决策落实。✅
- 两份 sample yaml 只加 `vertical/activity` 两键，无新段。✅
- e2e paths set 7→10 键，新增用例 pin 合法 ISO，旧文件字节对比保持。✅

**「无偏差」属实。**

---

## 阻塞项

**无。**

---

## 建议栏（非阻塞，不立案，交设计裁量）

1. **`profile_accounts` 单测中「部分笔记缺时间」场景用「全缺时间」代替**：§6 要求「`window_days=30` + 部分笔记缺时间 → `recent_note_count` 只含窗内数」，测试中 A2 只有 1 条 `published_at=""` 笔记（全部缺时间），验证了「缺时间不进 recent」行为，但「部分」语义（同一账号有多条笔记、其中一些有效时间、另一些缺时间）未精确覆盖。可补一条测试：A 账号 3 条笔记（N1 窗内有时间、N2 缺时间、N3 出窗），断言 `recent_note_count==1`。当前行为正确，仅测试覆盖精度不足，非缺陷。

2. **`test_pipeline_without_watchlist_does_not_call_fetch_creator_notes` 用路径断言代替 `profile_accounts` mock 断言**：§6 原文说「mock 断言」，实际落地为 `"account_profile" not in paths`，行为等价但形式偏差。下次评审或补测时可用 `monkeypatch.setattr("src.pipelines.run_research.profile_accounts", ...)` 配合 `call_count==0` 做严格 mock 断言，以与 §6 描述完全吻合。非阻塞。

---

## 验证记录汇总

1. 预飞独立复跑：全绿（22 项，命令 5/5、产物 10/10、符号 6/6、禁留 0）。
2. lint/format/pytest 独立跑：全绿，106 passed（起点 `c933f9e` 101 + 本期新增 5）。
3. no-touch 亲跑：`git diff --quiet c933f9e -- [6 路径]` → exit 0（非仅引用预飞）。
4. 夹带扫描：src 侧仅 4 个白名单文件；no-touch 零 diff；sample yaml 只加 2 键；fixture 只增 1 行；uv.lock/pyproject.toml 零 diff。
5. `_write_report` 零改动亲核：diff 无 `-` 行，函数体仅出现在上下文（`@@ -59,6 +61,91 @@` 前 1 行），`report_input.md` 字节对比 e2e 通过。
6. 格式精度亲算：`2/3=0.6666...` → `:.4f` → `"0.6667"`（四舍五入正确）；`18.666` → `:.2f` → `"18.67"` 正确。
7. B-1 亲核：603d 为新账号，期2 e2e watchlist 不含 603d，fixture diff 只有一行 `+`，pytest e2e 11 passed。
8. 合法 ISO pin 亲核：新增用例 pin `"2026-07-03T00:00:00+00:00"`，`datetime.fromisoformat` 可解析，age_days 计算正确；旧 watchlist e2e pin `'2026'` 但 window_days 默认 0，不触发 ValueError。
9. watchlist 权重合并亲算：`{**DEFAULT_PROFILE_WEIGHTS, **ranking_weights_no_va}` → vertical/activity 保留。
10. §6 测试逐一点名：17 类测试要求均有对应用例（见「§6 测试覆盖核对」节），2 条建议级偏差记录。
