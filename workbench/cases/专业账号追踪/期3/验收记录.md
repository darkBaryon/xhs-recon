---
title: 专业账号追踪 期3 验收记录
type: 验收记录
case: 专业账号追踪
phase: 3
created: 2026-07-03
---

## 验收结论

**通过**。期3「专业度打分与选题素材」交付：`profile_accounts` 分项打分（垂直度 = creator 笔记命中领域关键词占比；活跃度 = 窗内发帖数；综合分可复算）、`account_profile.csv`（蓝图逐字 5 列）、`topic_feed.md/jsonl`（仅窗内笔记，头部计数对账）。`report_input.md` 与旧 7 件导出零变化；src 仅动白名单四文件，adapters/fork/端口零 diff。**至此蓝图三期全部交付。**

## 验收方式

**复算实测**（Gate 2 第 3 档，方案 §6 口径，离线、无采集）：对期2 真实产物 `data/exports/20260702T163155/creator_notes.csv`（42 条）直接调 `profile_accounts` 复算——路觅账号垂直度 0.3000 = 明细命中 3/10 人工可数、窗内 8 条人工可数、综合分 11.00 = 10×0.3+8 手算吻合；`topic_feed` 对账 `kept 30 + 出窗 12 + 缺时间 0 = 42`；**2023 置顶旧帖被窗过滤剔除**（期2 已知项 1 的闭环实证）。此前 fixture demo、pytest **106 passed**（起点 101 + 新增 5 组）、ruff、预飞 24 项全绿均由代码评审独立复跑核实。

## 过程要点

- 分工：Claude 出方案 + 组织子代理评审（sonnet 评审 / haiku 机械核对），**Codex 实施**（5 小步 commit `b58fb0c..3c699c8` + docs `85fafaa`，纯 xhs-recon 单仓）。
- 方案评审两轮：#1 需修改（阻塞：e2e 窗内断言用例若沿用期2 `'2026'` pin 会 ValueError 崩溃，v2 钉死合法 ISO pin + 先例指向）+ 3 建议吸收；#2 通过（新增建议 B-1 出窗夹具行挂新账号，随实施落实）。
- 代码评审一轮**通过**（无阻塞，2 建议留档）：逐 commit 对账吻合、`_write_report` 零 diff 机械证明、独立复跑全绿。
- `profile_score` 落 `AccountRank` 模型属契约微扩，方案评审 #1 判定成立（csv 契约本身含该列），蓝图验证备忘登记，无需修蓝图。

## 已知项（非阻塞，留档）

1. **垂直度文本包含命中偏严的实例**：「Alevel经济张老师」垂直度 0.0（内容为课程码/经济术语，不含关键词字面子串）——v1 精度口径（蓝图「允许各期调，不算偏差」）；收敛路径：config 加领域同义词即可重算，无需改代码。
2. 单测「部分缺时间」用全缺时间账号代替、无 watchlist 跳过用路径断言代替 mock 断言（代码评审 2 条建议，行为等价，留档）。

## 合入

`feat/pro-account-tracking-phase3` → `main`（`--no-ff`），推送 origin。

## 蓝图进度

**期3/3 交付，蓝图 v2 三期全部收官**：期1 时间维度修复 → 期2 creator 拉取与 watchlist → 期3 专业度打分与选题素材。源需求「专业账号追踪与最新发帖」流转**已交付**。三期契约均经实施验证够用，蓝图无需出 v3。
