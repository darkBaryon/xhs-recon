---
title: 专业账号追踪 期1 验收记录
type: 验收记录
case: 专业账号追踪
phase: 1
created: 2026-07-02
---

## 验收结论

**通过**。期1 时间维度修复交付：MediaCrawler fork 新增可选 `--sort_type` CLI 透传（缺省不覆盖）、管线时间窗过滤（`search.window_days`，aggregate 前，三类计数）、选帖时间衰减（`selection.half_life_days`，半衰公式）。默认配置全 0 时旧行为与旧导出契约零变化，新能力全部 opt-in；`aggregate/account_ranker/parsers` 对起点零 diff（checklist no-touch 机械兜底）。

## 验收方式

**实测**（Gate 2 实测档，真实采集）：`sample_mediacrawler.yaml`（sort=time_descending + window=30 + half_life=14）真实跑通——40 条导出笔记 `published_at` **全部落在 2026-06-29 ~ 2026-07-02（近 3 天）**，改造前同关键词首条为 2024 年万赞帖；窗过滤统计 `kept=60 out_of_window=0 missing_time=0` 合理（时间序下前 20 条/词天然在窗内）。此前 fixture demo、pytest **65 passed**（起点 52 + 新增 13）、ruff、预飞 **17 项全绿**均通过。本跑同时关闭代码评审标注的唯一遗留项（fork `--sort_type` 端到端覆盖）。

## 过程要点

- 分工：Claude 出方案+组织评审，**Codex 实施**（xhs-recon 仓 4 小步 commit `874070e..7a8b4a5` + docs `ab3b444`；MediaCrawler 仓单 commit `9e17a5e`，双仓留痕）。
- 方案评审两轮：#1 需修改（阻塞：方案 §2 fixture 时间分布断言被评审实测证伪，v2 更正）+ 4 建议全吸收（e2e pin 口径、no-touch 机械兜底、负 age clamp、边界等号）；#2 通过。
- 代码评审一轮**通过**（无阻塞）：逐 commit 与方案 §4/§5 对账吻合、夹带扫描 0 命中（含 logging 关键字）、独立复跑预飞/测试全绿；实施偏差 3 条均如实且已关闭或可接受。
- 并行线说明：日志系统（独立需求，已单独 Gate 2 验收通过）在本分支以 merge `f49f313` 汇入——实测中的中文日志/日志文件来自该线，非期1夹带。

## 已知项（非阻塞，留档）

1. **日志不记采集命令行**：`FetchResult.command` 未入 DEBUG 日志，排查参数问题缺直接证据（本次靠单测+结果间接证明）——一行改动，记入期2方案复用盘点顺手带上。
2. **CDP 回落**：实测时 9222 端口 503，回落标准浏览器模式仍成功——浏览器未按调试端口启动所致，非缺陷（启动方式见 v0 期3 验收记录 CDP 口径）。
3. fork `--sort_type` 校验未按平台门控（非 xhs 平台也写 `config.SORT_TYPE`）——代码评审建议1，个人自用影响面小，留档不改。
4. 时间序混入蹭词水帖属蓝图既定风险，由期3 专业度打分解决，不在本期口径。

## 合入

`feat/pro-account-tracking-phase1` → `main`（`--no-ff`；随分支一并入主线的还有日志系统 merge `f49f313`，该线已独立验收）。

## 蓝图进度

期1/3 交付（时间维度修复）。期2（creator 拉取与 watchlist）待开方案——前置 spike：真实 creator 拉取验证纯 user_id 可用性。期3（专业度打分与选题素材）在期2 归档后开。蓝图 v2 契约经期1 验证够用，无需修蓝图（详见方案页蓝图验证备忘）。
