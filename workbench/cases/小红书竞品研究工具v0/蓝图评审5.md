---
title: 小红书竞品研究工具 v0 开发蓝图评审 #5
type: 方案评审
case: 小红书竞品研究工具v0
phase: 0
round: 5
verdict: 通过
created: 2026-06-24
---

## 结论

verdict: **通过**。v4→v5 差量正中第 4 轮的 1 阻塞 + 2 建议：源需求非目标**确已回写**（展开为「不自研爬虫／调用现成 MediaCrawler 低频小范围／禁 IP 代理池·CDP 反风控·并发增量」）且处置历史留痕，v5 §1 与回写后需求**逐条一致**；§3「零缺口」已缓和为「映射到位 + 零星空值降级」；§5 已订正 runner 硬编码路径失效、真实 MediaCrawler 在 lab 根。差量仅限这几处 + 变更节，**未夹带改契约／分期／依赖方向**。残留 1 条非阻塞瑕疵（runner 引用路径根歧义，沿袭自 v4）记建议栏。

## 差量逐项核查（只评 v4→v5 增量）

我对 v4↔v5 做了归一化版本号后的整文 diff（`diff <(sed 's/v4/vN/;s/v5/vN/') …`）：**实质差异仅 7 处**——frontmatter status、摘要数据源行、变更节、技术栈表数据源说明、§1 非目标、§3 契约注释、§4 期2 验收口径尾句、§5 两条风险。逐条核：

### 阻塞关闭：非目标回写 + 两份一致（第 4 轮唯一阻塞）
- **回写真实发生**：亲核 `reports/小红书竞品研究工具v0.md`。非目标已从旧「不做大而全爬虫」展开为 5 条（文件第 30-34 行）：
  - 「**不自研爬虫**，不做大而全采集；采集仅**调用现成 MediaCrawler 做低频、小范围**抓取（期2 数据源，用户裁决）」
  - 「不做绕风控/代理池/验证码——**含禁用 MediaCrawler 自带的 IP 代理池、CDP 反风控、并发/增量等能力**，只走其低频默认」
  - 互动/敏感字段/旧版本兼容三条保留。
- **处置历史留痕**：reports 第 53 行新增「2026-06-24 期2 数据源经横向调研定 MediaCrawler…据此细化非目标口径：调用现成 MediaCrawler 但**禁用其代理池/CDP 反风控/并发**，只低频小范围（回写自蓝图评审 #4 阻塞项）」。明确标注来源=阻塞项，留痕到位。
- **两份逐条一致**：v5 §1 非目标（第 51 行）=「不自研爬虫、不做大而全采集；调用现成 MediaCrawler 做低频小范围；**禁用其 IP 代理池 / CDP 反风控 / 并发 / 增量**；不做验证码绕过、持久化数据库、任何互动写操作」。与回写后需求**逐词对齐**——「不自研／大而全／低频小范围／禁代理池·反风控·并发增量」四个争议口径在两份文档措辞一致，无「一份松一份紧」。
  - 核对 v5 §1 标题亦从 v4 的「（蓝图层显式不做）」改回「（**与需求非目标一致**）」——主动声明对齐，不再是单方面口径。
  - 小注：v5 §1 未逐字列「不存评论敏感字段」，但该项在 §3 模型注释、§4 期3、§5 自律条均有；属正常的章节分工，非松绑。**不构成不一致。**
- **结论：第 4 轮阻塞本质已关闭**——需求确已回写、口径展开为可对账形态、两份非目标逐条一致。

### 措辞修正1：§3「零缺口」→「映射到位 + 零星空值降级」
- 亲核 §3 契约代码块 diff：v4「Note 字段…**全部有数据**」→ v5「**全部映射到位**」，并**新增一行**「（零星原始空值如个别 tag_list/comment_count，按 parser 既定降级为 []/0）」。
- 与第 4 轮实测（tag_list 第13条空、comment_count 第16条空、被 parser 降级为 []/0）严格对齐。措辞缓和到位，未夸大。
- 同步：摘要/变更节也用「映射到位」措辞，全文无残留「零缺口」强断言（grep 确认 §3 代码块外无「零缺口」字样）。

### 措辞修正2：§5 runner 硬编码路径失效 + 真实位置
- 亲核事实：runner 文件实际在 `/Users/xinyue/VSCode/ws_2026/xhs-recon-lab/data/.mediacrawler_xhs_runner.py`，其第 5 行 `repo = Path('/Users/xinyue/VSCode/ws_2026/xhs_mediacrawler_demo/MediaCrawler')`——该路径 `ls` = **No such file**（失效，实测）。真实 MediaCrawler：`ls /Users/xinyue/VSCode/ws_2026/xhs-recon-lab/MediaCrawler` = **存在**。
- v5 §5（第 100 行）已订正：「`…/xhs_mediacrawler_demo/MediaCrawler` **已失效**，真实 MediaCrawler 在 lab 根 `../MediaCrawler/`；spike 按真实位置摸…」。`../MediaCrawler` 相对 xhs-recon 根解析 = lab 根 MediaCrawler，路径正确。**修正到位。**
- 附带：v5 §5 自律条也从 v4 含糊的「MediaCrawler 声明仅供学习研究」改为「自带代理池/CDP 反风控等能力，本工具**显式禁用**」，与非目标回写一致，是正向收紧。

### 无夹带核查：契约 / 分期 / 依赖方向 / 技术栈
- **§3 契约**：代码块逐行 diff，除上述 2 行注释**无任何改动**——models（Account/Note/Comment/AccountRank/TypicalNote/FetchResult）、端口 `search(keyword,page,limit,collected_at)->FetchResult`、`fetch_comments` 默认 NotImplementedError、`.ok=not error`、5 文件导出契约全部原样。**未借机改接口。**
- **§2 依赖方向**：与 v4 字节一致（diff 无输出）——铁律 `pipelines→core→models`、core 无 "xhs"、两 adapter 共用 parsers 原样。
- **§4 分期表**：唯一差异是期2 验收口径尾部**新增**「解析/降级/raw 落盘**机械可验**」一句 + 「MediaCrawler 已在 lab」→「已在 lab 根」。前者是把第 4 轮已认可的机械可验部分显式化（非新增范围），后者是定位订正。交付物/分期结构/依赖前置无改动。
- **技术栈表**：仅数据源说明追加「禁代理池/反风控/并发」、刻意不上行不变。无新依赖、无依赖方向变更。

## 可证伪质疑

1. **【已关闭】第 4 轮阻塞——非目标是否真回写、两份是否一致？** 证伪测试：读 reports 非目标 5 条 + 处置历史；逐条比对 v5 §1。结果——reports 已展开为含「禁 IP 代理池/CDP 反风控/并发增量」的可对账口径，处置历史第 53 行标注「回写自蓝图评审 #4 阻塞项」，v5 §1 四个争议口径与之逐词对齐。**回写真实发生、两份逐条一致，关闭。**
2. **【已关闭】§3 措辞是否仍夸大「零缺口」？** 证伪测试：grep v5 全文「零缺口」。结果——§3 代码块及全文已无该强断言，改「映射到位 + 零星空值（tag_list/comment_count）降级 []/0」，与第 4 轮实测空值条目一致。关闭。
3. **【已关闭】§5 runner 路径修正是否属实？** 证伪测试：`ls` 旧硬编码路径 = No such file；`ls` lab 根 MediaCrawler = 存在；`../MediaCrawler` 相对 xhs-recon 根解析正确。v5 §5 表述与实测吻合。关闭。
4. **【已关闭】差量是否夹带改契约/分期/依赖？** 证伪测试：§3 代码块、§2 依赖块逐行 diff；§4 分期表比对。结果——契约 0 改动（仅 2 行来源注释）、依赖块字节一致、分期表仅显式化已认可的机械可验项 + 定位订正。无夹带。关闭。

## 建议栏（不立案，交设计职责）

- **（非阻塞，沿袭自 v4）runner 引用路径的根歧义**：v5 §5 写 `data/.mediacrawler_xhs_runner.py`，但该文件实际在 **lab 根** `xhs-recon-lab/data/`，**项目根** `xhs-recon/data/` 下并无此文件（只有 exports/processed/raw）。即引用路径相对哪个根不明。这是 v4 就在的瑕疵、本轮未引入也未恶化，且 §5 已点明「按真实位置实测」可兜住，故不阻塞；建议期2 方案落地时把该引用写全为 `xhs-recon-lab/data/.mediacrawler_xhs_runner.py` 以免 spike 找错目录。
- 呼应第 4 轮遗留建议：期2 方案应把「禁用 MediaCrawler 代理池/反风控/并发开关」做成可机械检查的 config 守护项，让回写后的非目标自律变成可验收（v0 蓝图层无需动）。

## 验证记录

- 读全文：v5、v4、第 4 轮评审、源需求 reports、评审规则、方案评审模板。
- **整文差量 diff**：`diff <(sed 's/v4/vN/;s/v5/vN/' 开发蓝图v4.md) <(… v5 …)` → 实质差异仅 7 处（frontmatter status、摘要数据源行、变更节、技术栈表说明、§1 非目标、§3 契约注释、§4 验收尾句、§5 两条风险），无其他改动。
- **§3 契约块单独 diff**：`diff <(sed -n '/# models/,/导出契约/p' v4) <(… v5)` → 仅「全部有数据→全部映射到位」+ 新增 1 行降级注释；models/端口/导出签名 0 改动。
- **§2 依赖块 diff**：无输出（字节一致）。
- **回写亲核**：读 reports 非目标第 30-34 行（5 条，含禁 IP 代理池/CDP 反风控/并发增量）、处置历史第 53 行（标注「回写自蓝图评审 #4 阻塞项」）；逐条比对 v5 §1 第 51 行，四争议口径逐词对齐。
- **runner 路径亲核**：`find` 定位文件在 `xhs-recon-lab/data/.mediacrawler_xhs_runner.py`；`grep` 其第 5 行硬编码 `xhs_mediacrawler_demo/MediaCrawler`；`ls` 该硬编码路径 = No such file；`ls xhs-recon-lab/MediaCrawler` = 存在；`ls xhs-recon/../MediaCrawler` 解析正确。
- **「零缺口」残留检查**：v5 全文已无「零缺口」强断言（已替换为映射到位 + 降级措辞）。
- `python3 tools/build_views.py`（workbench 根）：`OK: 17 篇文档，校验通过，视图已更新`，**EXIT=0**。
