---
title: 小红书竞品研究工具 期3 验收记录
type: 验收记录
case: 小红书竞品研究工具v0
phase: 3
created: 2026-07-02
---

## 验收结论

**通过**。期3 典型笔记评论交付：管线选典型后经 MediaCrawler `detail` 模式批量读一级评论，导出多产 `comments.csv`（恰四列 `body/note_id/like_count/collected_at`）并织入 `report_input.md`；`comments.enabled: false` 时与期2 行为一致。全部扩展 additive，`search/aggregate/rank/select` 与既有 5 文件列零改。

## 验收方式

**实测**（Gate 2 · B 档，真实采集）：CDP 连本机专用 Chrome（独立 profile + 9222），跑扩展后的集成脚本退出 0——真实采得 368 条评论（39 条典型笔记 × 上限 10），`report_input.md` 织入 114 行按赞数降序的评论；导出中 nickname/user_id/ip_location/avatar 0 命中（红线人工+grep 双核对）。此前 fixture demo（6 文件）、pytest 52 passed、ruff、预飞 15 项均绿。

## 过程要点

- 分工：Claude 出方案+组织评审，**Codex 实施**（5 个小步 commit `3782551..ff00b58`）。代码评审由独立子代理一轮通过（无阻塞项，3 条非阻塞建议留档于代码评审1）。
- **集成验收抓出并修 1 个真 bug**（offline 单测 mock 子进程测不出、真跑才暴露，延续期2 传统）：
  - `ee27910` MediaCrawler 子进程 stderr 含非法 UTF-8 字节（247KB 日志流）→ `subprocess.run(text=True)` 严格解码抛 `UnicodeDecodeError` 且穿透「失败装 error 不抛」契约炸管线；修复 = `errors="replace"` + except 扩为含 `UnicodeDecodeError`，附 2 个新单测。
- **CDP 环境口径更新**：Chrome 136+ 起 `--remote-debugging-port` 在默认 profile 上被静默忽略（安全策略），须配独立 `--user-data-dir` 并直接调二进制启动第二实例。本次已建专用采集 profile（`~/.xhs-recon-chrome`，登录态持久）。另清理了期2 遗留的僵尸 headless Playwright Chrome（占位导致 `open` 启动参数被吞）。

## 已知项（非阻塞，留作后续立案/裁决）

1. **典型笔记节流失效**：真实分布下账号多为 1-2 条笔记，「每账号 top2」使 39/40 条笔记皆为典型，detail 请求量≈全量——建议加典型笔记总量上限配置（待立案）。
2. **无日志系统**：子进程输出用后即丢（本次 247KB stderr 无据可查）、管线无过程可见性——待立案（方向：stdlib logging + `_run_crawler` 输出落盘 `{save_path}/mediacrawler.log` + 阶段 INFO）。
3. **raw 层评论含身份字段**：红线守在 parser 边界，`data/raw/*-comments/` 的 MediaCrawler 原始 JSONL 仍含评论者全字段（gitignore 本地留存）——是否脱敏/用后即删，口径待用户裁决。
4. 代码评审 3 条非阻塞建议（mutable default arg、0 条评论不产 CSV 的口径空白、limit=0 不截断）留档。

## 合入

`feat/phase3-comments` → `main`（`--no-ff`）。

## 蓝图进度

v0 三期全部交付：期1（fixture 管线）+ 期2（MediaCrawler 真实采集）+ 期3（典型笔记评论）。需求 → `已交付`，开发蓝图 v5 与期3 方案 → `已归档`，v0 收官。
