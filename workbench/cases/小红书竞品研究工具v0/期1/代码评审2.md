---
title: 小红书竞品研究工具 期1 代码评审 #2
type: 代码评审
case: 小红书竞品研究工具v0
phase: 1
round: 2
verdict: 通过
created: 2026-06-24
---

# 小红书竞品研究工具 期1 代码评审 #2（差量）

## 结论

**verdict: 通过。** 差量范围 = `ae0de6b`（删 `normalize_count` `"千"` 分支）+ `93a4ebd`（实施偏差记录订正#1 + 补登#3 修复）。第 1 轮唯一阻塞（`"千"` 投机分支）已删净、无残留、无回归；要求的偏差 #1 性质订正已到位且更进一步澄清了**精确复现条件**（反而把第 1 轮「本机不可复现」的疑点彻底闭合）；修复已补登为偏差 #3。命令亲核全绿（ruff/pytest/e2e），无新引入问题、偏差记录再无失实。第 1 轮已通过部分不重复刨。

## 差量核查（带亲核证据）

### commit `ae0de6b` —— 删 `"千"` 进位分支

跨两笔 commit 的 src 净改动只有这 3 行（`git diff ae0de6b~1 93a4ebd -- src/`）：
```
-        if s.endswith("千"):
-            return int(float(s[:-1]) * 1000)
```
- **删净**：`grep -rn "千" src/adapters/parsers.py` → **exit 1，无输出**（0 处）。✅ 阻塞关闭。
- **现归一化路径仅三类**（读 `parsers.py` L12–24）：`"万"` 进位 / 纯数字 `int(float(s))` / `ValueError→0`，与方案 §4「万 / + / 纯数字 / 空异常」对齐，无第四类投机分支。✅
- **逐用例亲核**（实跑 `uv run python -c`，非采信）：`'1万'→10000`、`'10万+'→100000`、`'921'→921`、`''→0`、`'1.2万'→12000`、`None→0`、`'garbage'→0` 全 OK；`'5千'→0`（落入 `int(float('5千'))`→ValueError→0），即「千」取值现按「异常→0」处理，正是删分支后的预期行为，非崩溃。✅
- **无回归**：`test_parsers::test_normalize_count_variants` 仍断言保留的全部路径（万/小数万/万+/纯数字/空/None/garbage，L10–16）；删掉的 `"千"` 分支原本**无任何测试**，故删除不留孤儿断言、不破坏既有用例。`uv run pytest -q` → 28 passed（与第 1 轮同数，无用例增删）。✅

该 commit 同时改了 workbench 视图文件（SUMMARY/nav.js/index/all/by-case 登记代码评审1.md + 新增代码评审1.md 文件）——属流程文档登记，非 src，不影响交付。

### commit `93a4ebd` —— 偏差记录订正

仅改 `方案v2.md` 实施偏差记录节（`git show 93a4ebd` 确认无 src 触碰）：
- **偏差 #1 性质订正到位**（L273）：由旧表述「`grep "xhs"` 命中 `.pyc` 嵌入路径产生**误报**（源码实测 0 处）」改为「**防御性**修复，非稳定可复现的实测误报」，并补出**精确复现条件**：「仅当 `src/core/__pycache__/*.pyc` 存在时…预飞流程内『先跑 pytest 生成 .pyc → 后 grep』的顺序下会触发，但清缓存或不同时序下不复现」。这恰好闭合第 1 轮的疑点——第 1 轮判「误报本机不可复现」是因为亲核时 grep 未在「刚生成 .pyc」的时序下跑；订正后的描述把断言收敛到**条件性触发**，既不夸大为稳定误报、也不否认存在性，措辞诚实。✅
- **修复补登为偏差 #3**（L275）：`ae0de6b` 删 `"千"` 分支登记为「代码评审#1修复…按范围哲学去除，无残留」，描述与实际 diff 一致。✅
- 原 #3「无其他不符」顺延为 #4（L276），措辞不变，仍属实。

## 接口面对账（差量）

差量未新增/删除任何对外符号：`normalize_count` 签名 `(raw) -> int` 不变，仅删一内部行为分支（收窄行为面，不扩面）。第 1 轮认定的「唯一超出方案接口面者」即此分支，现已消除，接口面与行为面**完全 ⊆ 方案 §4/§5**。✅

## 可证伪质疑

1. **（关闭）** `"千"` 分支是否真删净、有无别处残留？→ `grep -rn "千" src/adapters/parsers.py` exit 1；全 src 净 diff 仅此 3 行删除，无补偿性代码潜入别处。关闭。
2. **（关闭）** 删分支是否改变保留路径的归一化结果（隐性回归）？→ 7 个保留用例实跑值与第 1 轮记录逐一相等；`'5千'` 现→0 是删分支的**预期**收敛（异常→0），非新 bug；pytest 28 passed 无变。关闭。
3. **（关闭）** 偏差 #1 订正是否仍失实（把不存在的误报说成存在）？→ 订正后限定为「`.pyc` 存在且 pytest-先-grep-后 时序」的条件触发，且 `-I` 是正向防御；这是**可证伪且更弱**的断言，未夸大。第 1 轮要求的「订正为防御性、复现依赖 .pyc 时序」已字面满足。关闭。
4. **（关闭）** 是否引入新偏差/新失实？→ 差量两笔 commit 的 src 改动仅删 3 行（不增功能、不加符号、不铺抽象）；docs 改动仅订正+补登，偏差记录现 4 条与实际一一对应，无漏登、无夸大。关闭。

未关闭阻塞项：**无**。

## 建议栏（评审不立案，交设计统一立）

- 第 1 轮建议栏两条（`keyword_hit_count` 语义在期2 spike 明确；偏差#1 的 `-I` 防御建议作为工作台工具改进单独走流程而非挂期1）本轮仍成立，差量未触及，留待 Gate2 / 期2 处置，此处不重复展开。

## 验证记录

环境：repo 根 `/Users/xinyue/VSCode/ws_2026/xhs-recon-lab/xhs-recon`，分支 `feat/phase1-fixture-pipeline`，`unset VIRTUAL_ENV` 后用 `uv run`。

```
$ git log --oneline -2
  93a4ebd docs(期1): 实施偏差记录订正#1描述+补登代码评审#1修复
  ae0de6b fix(期1): 删 normalize_count '千' 进位分支（代码评审#1：投机泛化）

$ grep -rn "千" src/adapters/parsers.py            → exit 1（0 处，删净）

$ git diff ae0de6b~1 93a4ebd -- src/               → 仅 parsers.py 删 2 行（"千" 分支）

$ uv run python -c "normalize_count 逐用例"
  '1万'→10000 '10万+'→100000 '921'→921 ''→0 '1.2万'→12000 None→0 'garbage'→0 '5千'→0  全 OK

$ uv run ruff check .                               → All checks passed!  (exit 0)
$ uv run pytest -q                                  → 28 passed in 0.09s  (exit 0)
$ rm -f data/exports/*; uv run python -m src.pipelines.run_research --config configs/sample.yaml
                                                     → exit 0；data/exports/ 下 5 文件真生成
  (account_rank.csv accounts.csv notes.csv report_input.md typical_notes.csv)
```

test_parsers 覆盖核查：`test_normalize_count_variants` 仍断言万/小数万/万+/纯数字/空/None/garbage 七路径（L10–16），删除的 `"千"` 分支原无测试，删后无孤儿断言。

偏差记录订正亲核：`git show 93a4ebd` —— 仅 `方案v2.md` 偏差节，#1 改为防御性+精确复现条件、#3 补登 `ae0de6b`、原#3 顺延为#4；无 src 触碰。

`python3 tools/build_views.py`（工作台根）→ 见交回（评审写入后跑）。
