---
title: 小红书竞品研究工具 期1 验收记录
type: 验收记录
case: 小红书竞品研究工具v0
phase: 1
created: 2026-06-24
---

## 验收结论

**通过**。期1 fixture 管线交付：12 步小步实现、每步独立 CI 全绿 commit；两轮代码评审收敛通过（#1 揪出投机泛化 → 修复 → #2 通过）；预飞 18 项全绿；`pytest` 28 passed + e2e + demo 实跑产出 5 导出文件。

## 验收方式

用户**实测**：打开 `data/exports/report_input.md` 查看端到端产出；确认评审结论为通过。

## 验收口径逐项（需求/方案 §7）

- ✅ `uv run python -m src.pipelines.run_research --config configs/sample.yaml` 端到端跑通；
- ✅ 产出 `accounts / notes / account_rank / typical_notes / report_input.md` 五件；
- ✅ 测试不依赖真实小红书访问（吃 `tests/fixtures/`，28 passed）；
- ✅ `core` 内 `xhs` 0 处（平台无关铁律，预飞守护）；
- ✅ 导出无评论用户敏感字段（期1 无评论；`Account` 仅 account_id/nickname）；
- ⏸ 「无 xhs 命令自动降级 fixture」属**期2** 交付，期1 不涉。

## Gate 决策结果

- **偏差①**（实施中改 `workbench/tools/preflight.py` 加 `-I --exclude-dir=__pycache__`）：用户验收时未否决设计「保留」建议，**裁决：保留**。理由：防御性消除 `.pyc` 嵌入路径噪声，对所有案子正向。
- **偏差②**（ruff `extend-exclude=["workbench"]`）、**偏差③**（删 `normalize_count` "千" 分支）：备案 / 已修复，无需裁决。

## 合入

`feat/phase1-fixture-pipeline` → `main`（`--no-ff`）。

## 蓝图进度

v0 共 3 期：**期1 已交付**；期2（真实 `xhs` CLI，开工前须 spike）、期3（评论，可选）待开。需求保持 `开发中`，开发蓝图 v3 未归档（待最后一期验收）。
