---
title: 小红书竞品研究工具 期1 方案评审 #2
type: 方案评审
case: 小红书竞品研究工具v0
phase: 1
round: 2
verdict: 通过
created: 2026-06-24
---

## 结论

第 1 轮 2 阻塞 + 3 建议**逐项亲核已全数关闭**，差量仅限这 4 处订正 + 「v1→v2 变更」节，未引入新问题、未顺手改动其他设计——**通过**。

## 逐项差量核查（仅评 v1→v2 增量）

先以 `diff 方案v1.md 方案v2.md` 锁定全部改动面（见验证记录）：除 frontmatter（version 1→2、status 需修改→待评审、标题）与新增「v1→v2 变更」节外，正文实质改动**恰为下述 4 处**，无第 5 处。

**阻塞1 — §1 `.gitignore` 行号订正：关闭。**
v2 §1 写「`.gitignore:16-18`」。亲跑 `grep -n "" .gitignore`：`data/raw/`=16、`data/processed/`=17、`data/exports/`=18，文件共 18 行（`wc -l`=18，无第 19/20 行）。v1 的 `18-20` 错引已订正为与实测一致的 `16-18`。「变更」节自述也写 16-18，自洽。✅

**阻塞2 — §7 ↔ checklist.yaml 对齐：关闭。**
v2 §7「禁留标记」现仅列 4 条：`xhs`(src/core)/`print(`/`proxy_pool`/`captcha`——与 `checklist.yaml:25-28` 的 4 条禁留标记**逐条一致**。v1 多出的「无自动互动符号残留」散文承诺已删除。互动等红线下移为 §8「设计意图」，并在 §7 末尾加引用块说明「因无单一稳定关键词可机械 grep，不进 checklist，由范围对账兜底」——口径合理、不再与机器表矛盾。`checklist.yaml` 未改（git 未跟踪该文件，diff 无从比对；但 v2 仅改 §7 散文向机器表靠拢，机器表本身无需动，「checklist.yaml 不变」承诺成立）。✅
附带：v2 同步把 §7 必过命令补全 `uv run` 前缀、必含符号 `src/models`→`src/models.py`——二者均是向 `checklist.yaml:6-9、21` 的精确措辞靠拢，属阻塞2「§7 严格对齐之」的应有之义，**不是范围外改动**，无设计变更。

**建议 — §2 README/MediaCrawler 归因订正：关闭。**
v2 §2 删去「对齐 MediaCrawler…（README.md 述其为数据源）」，改为「`parsers` 对齐该 fixture 实测的 21 字段形状…不依赖外部文档归因」。亲跑 `grep -n -i mediacrawler README.md` → 0 命中（exit 1），佐证 README 确无此字样，错误归因已清除。✅

**建议 — §4 synonyms 标注可演进：关闭。**
v2 §4 `keyword_expander` 行尾加「**synonyms 为 v0 占位扩展策略，允许各期演进**」；§4 config 形状的 synonyms 字段不变。蓝图未具名 synonyms，标注为占位可演进可避免后续误判为契约。✅

**无新问题确认。**
diff 显示算法（aggregator/ranker/selector 公式、权重 10/5/0.01）、数据模型、端口签名、导出契约、实现步骤、测试用例、风险/回滚**全部逐字未变**，与 v1 已通过部分一致。订正均为文档措辞层，无一处触及接口面或行为。

## 可证伪质疑

1. **[已关闭]** v2 §1 的 `16-18` 是否真实？`grep -n "" .gitignore`：16=data/raw/、17=data/processed/、18=data/exports/，文件 18 行——属实。
2. **[已关闭]** v2 §7 删散文后是否与 checklist 再无矛盾？逐条比对：§7 四条禁留标记 == checklist.yaml 四条；无任何 §7 独有而机器表缺失的守护项——一致。
3. **[已关闭]** README 是否确无 MediaCrawler 字样（支撑「错误归因」判定）？`grep -i mediacrawler README.md` 0 命中——属实，删除归因正确。
4. **[已关闭]** 4 处订正之外是否夹带其他设计改动？全文 diff 仅这 4 处 + frontmatter + 变更节；§7 的 `uv run` 与 `.py` 补全均落在阻塞2「对齐 checklist」范围内，非新增设计——无夹带。

## 建议栏（不立案）

- 无新增范围外问题。第 1 轮建议已全部消化；本轮无遗留可证伪质疑。
- （备忘，非阻塞）`checklist.yaml` 未被 git 跟踪，故「checklist 不变」无法用版本差佐证，仅能从 v2 仅改 §7 散文向机器表靠拢推断；后续若机器表需改，应同轮在评审中对账。

## 验证记录

仓库根 = `/Users/xinyue/VSCode/ws_2026/xhs-recon-lab/xhs-recon`；工作台根 = 其下 `workbench/`。

- `diff workbench/.../方案v1.md workbench/.../方案v2.md`：实质正文改动恰 4 处（§1 行号、§2 归因、§4 synonyms、§7 禁留标记+必过命令前缀+符号路径）+ 新增「v1→v2 变更」节 + frontmatter（version/status/标题）；算法/模型/端口/导出契约/步骤/测试/风险/回滚段**逐字未变**。
- `grep -n "" .gitignore` → 共 18 行；`data/raw/`=16、`data/processed/`=17、`data/exports/`=18；`wc -l`=18（第 19/20 行不存在）→ v2 写 `16-18` 正确（阻塞1 关闭）。
- `grep -n -i mediacrawler README.md` → 无命中（exit 1）→ v2 删除 MediaCrawler 归因有据（建议 README 关闭）。
- §7 ↔ `checklist.yaml` 逐条对账：禁留标记 v2 四条 == 机器表四条（xhs/print(/proxy_pool/captcha）；必过命令、必含符号措辞 v2 已与机器表一致（阻塞2 关闭）。
- `git ls-files .../checklist.yaml` → 空（未跟踪），无版本差可比对；据 diff 推断 checklist 未改。
- `python3 tools/build_views.py`（工作台根，写本评审后复跑）→ `OK: ... 校验通过，视图已更新` EXIT=0。
