---
title: 小红书竞品研究工具 v0 蓝图评审 #3
type: 方案评审
case: 小红书竞品研究工具v0
phase: 0
round: 3
verdict: 通过
created: 2026-06-24
---

## 结论

v2→v3 仅两处增量：§3 `Account` 删 `profile_url`（方案 B，关闭第 2 轮唯一阻塞）、期1 验收口径补「fixture 含重复作者+多关键词」（验收口径强化、非范围扩张）。两项均干净落地、自洽、无悬空引用、无新问题，verdict = **通过**。第 2 轮唯一阻塞（profile_url 可用性未证）已通过「方案 B：不存该字段」关闭。

## 逐章核查（差量）

亲跑 `diff v2 v3`，实际改动仅 8 处行块，逐处归类：①标题/status；②摘要新增 account_id 身份行；③v2→v3 变更节重写；④§2 铁律「v2 把→v2 起把」措辞微调；⑤§3 Account 签名删字段；⑥期1 验收口径追加 fixture 子句；⑦§5 新增 fixture 代表性风险条；⑧验证备忘 v3→v4。无其他隐藏改动。

**变更1（§3 `Account` 删 `profile_url`）—— 关闭第 2 轮唯一阻塞。**
- 字段移除已落到模型签名：`Account(account_id, nickname, source_keywords[], note_count, first_seen_at, last_seen_at)`，对比 v2 少了 `profile_url`，且原 `profile_url = adapter 由 user_id 拼…` 注释行整行删除。亲核 `grep -n profile_url v3` = **4 命中**，逐条核查：line 25（摘要）、line 29（变更节标题引用第 2 轮阻塞名）、line 31（变更项正文「删除…故 v0 不存」）、line 81（§3 Account 注释「v0 不存 profile_url」）——**4 处全是「我们删了它、且为何删」的决策留痕，无一处把 profile_url 当作活字段/依赖**。即「彻底移除」指的是字段从契约面消失，留痕是规则要求的「v+1 文档记录变更」，二者不矛盾。
- **悬空引用排查**：广扫 `主页 / /user/ / url`。§2 依赖图从未出现 profile_url；AccountRank/TypicalNote/Note 三模型均无该字段（Note.url/TypicalNote.url 是 `note_url` 派生，与用户主页 URL 无关，line 84/87/91 自洽）；导出契约（line 114）是文件名级（`accounts.csv …`），无 accounts 的逐列 url 承诺，故删字段不留断列。**别处无任何「假设 profile_url 存在」的悬空假设。** ✅
- **与「唯一标识 = account_id」自洽**：摘要 line 25 + §3 line 81 双处钉死 `account_id = user_id` 为账号唯一标识，删 profile_url 后账号去重/排名全程认 account_id，不依赖任何 URL。✅
- **是否真正关闭第 2 轮阻塞**：第 2 轮阻塞内核 = 「拼接式 profile_url 无 xsec_token、可用性未证，却以跨期稳定契约之名承诺」。方案 B 直接不存该字段，承诺消失，疑点自然消解；并在变更节给出依据（搜索载荷只含带 token 的 note_url、无主页 URL/token——与第 2 轮亲核样本一致），且预留「将来由 detail/creator 模式补、届时再扩契约」的扩展口，不堵死后路。**阻塞关闭。** ✅

**变更2（期1 验收口径补「fixture 含重复作者+多关键词」）—— 验收口径强化，非范围扩张。**
- **强化而非扩张**：源需求 §目标 step 3/4（提取 notes+accounts、对账号**去重**打分）与 step 1（**扩展关键词**）本就把「去重 / 跨关键词合并」列为 v0 in-scope 行为；v3 只是要求 fixture 真正触发这两条已承诺逻辑，属验收严谨度，未引入任何新功能/新接口/新依赖。对照第 2 轮建议栏与質疑#3——这正是第 2 轮明确指引「期1 checklist 必须含重复作者+多关键词命中同 note」的落地，方向一致。
- **与源需求验收口径无冲突**：源需求 §验收口径是文件输出级（端到端跑通、降级、产出导出文件、不含敏感字段），未对 fixture 构成做任何相反约束；v3 子句是对其的下钻补强，不抵触。
- **表述机械可验**：「含重复作者」= fixture 中 distinct(user_id) < 行数；「多关键词命中同 note」= 同一 note_id 跨 ≥2 个 source_keyword。两者皆可由一段脚本判真伪。亲核现状证此补强必要：当前 fixture 5 行 / distinct user_id = **5** / source_keyword = **{留学辅导}**（单一），去重与合并在验收里确会全程空转——v3 §5 风险条与期1 口径如实记录了这一缺口，并把补合成样本的责任明确落到期1。✅

## 可证伪质疑

1. **（§3 / Account，已关闭）** 「删 profile_url 后是否仍有模块假设它存在？」可证伪点：grep v3 全文除决策留痕外是否存在「读取/拼接/导出 profile_url」的活引用。亲核 = 0 处活引用（4 命中全为删除说明）；AccountRank/TypicalNote/导出列均不含主页 URL。**关闭**。
2. **（§4 / 期1 fixture，已关闭，非阻塞）** 「v3 只改口径文字、未改 fixture 文件，期1 验收会不会仍拿旧 5 条单关键词样本过关？」可证伪点：现 fixture distinct user_id=5、单关键词，确不满足新口径。但 v3 把「补含重复作者+多关键词的合成样本」写进期1 验收口径**与** §5 风险条，责任明确落在期1 实施/checklist；蓝图层只钉口径不造数据，符合分层。**非阻塞**——留给期1 方案，但期1 若不补样本即不满足自身验收口径，可被机械拦下。
3. **（§3 / 扩展口，已关闭）** 「『将来由 detail/creator 模式补主页链接』是否构成 v0 隐性承诺/范围扩张？」可证伪点：该句是否落到任何 v0 交付物或接口面。亲核：仅出现在变更说明散文，未进 §3 契约、§4 分期表、导出契约，属未来方向声明而非 v0 承诺。**非扩张**。

## 建议栏（不立案）

- 期1 方案落地合成 fixture 时，建议同时覆盖第 2 轮質疑#2 留的「adapter 出半成品 Account（note_count/source_keywords[] 缺省值）→ core 补全」中间态，避免 pydantic 必填校验在 adapter 边界炸（v3 未触及此点，沿用 v2，仍属期1 实现细节）。
- 第 2 轮建议栏的 avatar「采集即丢弃 + grep 守护」在 v3 三模型仍无显式落字；建议期1 方案补一条，防头像 URL 漏进 accounts.csv（与本轮两项增量无关，仅延续提示）。

## 验证记录

差量评审，均在项目仓库根 `/Users/xinyue/VSCode/ws_2026/xhs-recon-lab/xhs-recon`（绝对路径/先 cd 规避 cwd 误判）与工作台根下亲跑：

- `git rev-parse --show-toplevel` → `.../xhs-recon`（仓库根确认）。`ls src configs` → `src` 仍不存在、`configs` 空，符合「未开工」。`which xhs` → not found, exit=1（期2 需 spike 仍属实）。
- `diff 开发蓝图v2.md 开发蓝图v3.md` → 实际改动 8 处行块，与 v3 自称 2 项增量（+ 标题/status/备忘等机械改）一致，无夹带。
- `grep -n profile_url 开发蓝图v3.md` → **4 命中**（line 25 摘要 / 29 变更节标题 / 31 变更项正文 / 81 §3 Account 注释），逐条确认**全为删除决策留痕，0 处活字段/依赖引用**。广扫 `主页|/user/|url` → 仅 Note.url/TypicalNote.url（note_url 派生，与主页无关），无悬空 profile_url 假设。
- 核 §3 Account 签名：v3 = `Account(account_id, nickname, source_keywords[], note_count, first_seen_at, last_seen_at)`，较 v2 删 `profile_url` 及其来源注释行；账号唯一标识双处钉 `account_id = user_id`，自洽。
- 解析 fixture（`tests/fixtures/search_contents_sample.jsonl`，wc -l=5）：rows=5、distinct user_id=**5**、source_keyword=**{留学辅导}**（单一）→ 证 v3「现样本无法触发去重/合并、须补合成样本」的事实判断为真，新增口径有实据。
- `tools/build_views.py` 校验规则：方案评审必填 `case/phase/round/verdict`，verdict ∈ {通过,需修改,驳回}（本评审 frontmatter 合规）。
- `python3 tools/build_views.py`（工作台根）→ **exit 0**（写入本评审后复跑，见交回）。
