---
title: 小红书竞品研究工具 期2 代码评审 #2
type: 代码评审
case: 小红书竞品研究工具v0
phase: 2
round: 2
verdict: 通过
created: 2026-06-24
---

## 结论

**通过**。差量评审仅看 fix commit `6b9778b`（第 1 轮「需修改」后的修复增量），代码改动严格限于 `src/adapters/mediacrawler_adapter.py`（launcher 可配）+ `tests/test_mediacrawler_adapter.py`（超时/异常 + launcher 测试）+ 方案 v2「实施偏差记录」第 3 条登记 uv 偏差并标 Gate2 裁决。第 1 轮唯一阻塞（命令硬编码 `uv run` 未登记）已闭环：偏差已登记并交 Gate2、`_build_command` 改用可配 `launcher`、第 1 轮非阻塞建议（超时/异常分支无测）已补测。四必过命令全绿（`ruff check`/`format --check`/`pytest -q` → 38 passed，比上轮 +2），无新问题、无夹带（契约/provider 分支/合规开关零改动）。

## 对照方案逐 commit 核查（差量＝fix commit `6b9778b`）

亲核 `git show HEAD --numstat -- src/ tests/`：代码侧恰 2 文件 —— adapter `5+/3-`、test `23+/0-`。该 commit 另含 workbench 内第 1 轮评审文档与 views 重建（generated），非代码、不影响 verdict。

### 阻塞① uv 偏差登记 + Gate2 裁决

亲读方案 v2「实施偏差记录」第 3 条（line 167）：

> 「**代码评审 #1 修复**：命令前缀用 `uv run python`（非方案 §4 字面 `python main.py`）——MediaCrawler 以 uv 管理（有 `uv.lock`/`pyproject`），须用其环境跑以带 Playwright；已做成 `launcher` 参数**可配**（默认 `["uv","run","python"]`，可换其 .venv python）降硬依赖，并补超时/异常分支测试。**请 Gate2 裁决 uv 前缀去留**。」

第 1 轮阻塞要求「登记裁决或回退」。此处已登记进方案「实施偏差记录」（第 3 条），交代了 uv 前缀理由（MediaCrawler 以 uv 管理依赖 + 带 Playwright），并**显式标了 Gate2 裁决**。登记要件齐。✅

### 阻塞② `_build_command` 改可配 launcher（不再硬编码）

亲读 `src/adapters/mediacrawler_adapter.py:33,43,51`：
- `__init__` 新增 kwarg `launcher: list[str] | None = None`（line 33）；
- `self.launcher = launcher or ["uv","run","python"]`（line 43，默认值即第 1 轮硬编码的三元组）；
- `_build_command` 第一段由硬编码 `"uv","run","python"` 改为 `*self.launcher`（line 51）。

**live 亲跑验证**（独立，非采信测试）：
- 默认构造 → `_build_command(...)[:4] == ['uv','run','python','main.py']`（与方案 §4 既定 uv 前提一致）；
- `launcher=['/path/.venv/bin/python']` → `[:2] == ['/path/.venv/bin/python','main.py']`。
硬依赖 uv 已解除，可换为 MediaCrawler `.venv` 的 python。✅

### 第 1 轮非阻塞建议③：超时/异常分支补测

亲读 test 新增两条：
- `test_search_subprocess_exception_is_error`：monkeypatch `_run_crawler` 抛 `subprocess.TimeoutExpired(cmd, 1)`，断言 `not r.ok and "run failed" in r.error`。**亲核 `subprocess.TimeoutExpired` 确为 `subprocess.SubprocessError` 子类**（live：`issubclass(...) → True`），故被 adapter `search` 的 `except (OSError, subprocess.SubprocessError)`（line 117）捕获，走 `_err("run failed: …")`（line 118）。方案 §6 点名的「超时」覆盖闭合。✅
- `test_launcher_default_and_configurable`：断言默认 `["uv","run","python","main.py"]` 与可配 `["python3"]` 两路径。✅

两测均 offline（不实跑、不联网）：异常测注入 `_run_crawler`、launcher 测只调 `_build_command` 纯字符串拼装。

## 接口面对账（差量）

- 新增对外面：仅 `MediaCrawlerAdapter.__init__` 多一个**可选** kwarg `launcher`（默认值保持第 1 轮行为，向后兼容）。无新增导出符号、无新增模块、无新依赖（仍 stdlib `re`/`subprocess`/`pathlib`）。⊆ 方案 §4 接口面（`launcher` 是 §4 既有 `*` keyword-only 参数族的同性质扩展，且为闭合第 1 轮阻塞所必需）。✅
- **无夹带**（亲核 `git show HEAD --stat` 限定契约/provider 文件 → 输出空）：`src/models.py`、`src/adapters/parsers.py`、`src/core/*`、`src/adapters/fixture_adapter.py`、`src/pipelines/run_research.py` 在本 commit **零改动**。provider 分支、合规开关（`--enable_ip_proxy no`/`--max_concurrency_num 1`/CDP 不入命令）原样保留（亲核 grep adapter line 65-68 仍在）。✅

## 可证伪质疑

1. **【已闭】launcher 默认值是否退化了第 1 轮已验过的命令构造？**
   可证伪点：若默认 `launcher` 与第 1 轮硬编码不一致，命令会变。亲核 `self.launcher = launcher or ["uv","run","python"]`，默认三元组逐字等于第 1 轮 `"uv","run","python"`；live 跑默认构造 `[:4]==['uv','run','python','main.py']`，且 `test_build_command_has_compliance_flags`/`test_cookies_appended_only_when_set` 两条第 1 轮已通过的命令构造测试在本轮仍 PASS（7/7）。默认路径行为不变，仅多出可配入口。**已闭，非阻塞。**

2. **【已闭】异常测是否真打到 `except` 分支，而非借空输出/非 0 走 `_err`？**
   可证伪点：若 `TimeoutExpired` 不被 `except (OSError, subprocess.SubprocessError)` 捕获，会逃逸为未捕获异常、测试报 error 而非断言 `run failed`。亲核：`TimeoutExpired ⊂ SubprocessError`（live `True`），`_run_crawler` 注入直接 `raise` → 命中 line 117 `except` → `f"run failed: {e}"`（line 118）；测试断言 `"run failed" in r.error` PASS。该测确切覆盖第 1 轮缺口的「超时」分支。**已闭。**

3. **【已闭·非阻塞遗留】未知 provider 静默落 FixtureAdapter（第 1 轮质疑2）本轮是否被动？**
   亲核本 commit `run_research.py` 零改动，该行为未变（仍与方案 §4 伪码字面一致，非偏差）。不在本差量范围，维持第 1 轮「记建议栏、非阻塞」结论。**无新问题。**

**无未关闭阻塞项。**

## 建议栏（不立案，交设计裁量）

- Gate2 若裁定保留 uv 前缀，建议方案 §4 伪码同步把 `python main.py` 注明「实际经可配 `launcher` 注入，默认 `uv run python`」，使伪码与实现彻底对齐（当前偏差记录已交代，但 §4 正文伪码仍是裸 `python`）。
- 第 1 轮建议栏未知 provider fall-through 加 `log` 警示一条仍适用（非本轮范围，留待后续）。
- 真实采集 manual 集成验收（集成脚本 + 登录前置）仍 `⏸ 待用户登录`，launcher/uv 前缀的**运行时正确性**仍未经真实采集验证——属运行时盲区，offline 单测已尽其能；集成实跑时一并观察 uv 环境是否真带上 Playwright。

## 验证记录

跑命令前 `unset VIRTUAL_ENV`、用 `uv run`；repo 根 `xhs-recon/`。

1. **差量范围**（亲跑 `git show HEAD --numstat -- src/ tests/`）：代码侧恰 2 文件 —— `src/adapters/mediacrawler_adapter.py` 5+/3-、`tests/test_mediacrawler_adapter.py` 23+/0-。`git show HEAD --stat` 限定契约/provider 文件（models/parsers/core/fixture_adapter/run_research）→ **输出空**（零夹带）。
2. **四必过命令全绿**（亲跑）：`uv run ruff check .` → `All checks passed!`（exit 0）；`uv run ruff format --check .` → `29 files already formatted`（exit 0）；`uv run pytest -q` → `38 passed`（exit 0，较上轮 36 +2 = 新增两测）。
3. **新测真覆盖**（亲跑 `pytest tests/test_mediacrawler_adapter.py -v`）→ 7 passed，含 `test_search_subprocess_exception_is_error`、`test_launcher_default_and_configurable`。
4. **TimeoutExpired 归类**（live）：`issubclass(subprocess.TimeoutExpired, subprocess.SubprocessError) → True`，确被 adapter `except` 捕获 → `run failed` 路径。
5. **launcher 默认/可配**（独立 live）：默认 `_build_command(...)[:4] == ['uv','run','python','main.py']`；`launcher=['/path/.venv/bin/python']` → `[:2] == ['/path/.venv/bin/python','main.py']`。硬依赖解除。
6. **偏差登记**（亲读方案 v2 line 167）：「实施偏差记录」第 3 条登记 uv 前缀，标「请 Gate2 裁决 uv 前缀去留」，登记要件齐。
7. **build_views**：写入本评审后跑 `python3 tools/build_views.py`（工作台根）= exit 0（见交回）。
