---
title: 小红书竞品研究工具 期2 验收记录
type: 验收记录
case: 小红书竞品研究工具v0
phase: 2
created: 2026-06-24
---

## 验收结论

**通过**。期2 接入真实数据源 MediaCrawler，端到端跑通：给定「留学辅导」真实采集 → adapter 跨 cwd 读回 → 复用期1 parsers → 聚合/打分/选典型 → 产出 5 个导出文件。`core`/`models`/`parsers`/`exporter` 零改动，仅新增 `mediacrawler_adapter` + provider 分支。

## 验收方式

**实测**（manual 集成验收）：登录后真实采集，`report_input.md` 含真实竞品账号排名（菜鸡罢了 127.63 / 路觅留学生课程辅导 41.97 …），笔记 URL 带真实 xsec_token 可点开；`notes.csv` ~70KB；raw 仅 `search_contents`、无评论文件。

## 过程要点

- 代码评审两轮通过（#1 揪出 uv 前缀未登记 → launcher 可配 + 登记；#2 通过）。
- **集成验收抓出并修 2 个真 bug**（offline 单测 mock 子进程未覆盖、真跑才暴露）：
  - `184c4c5` out_dir 绝对路径（MediaCrawler 以自身 cwd 跑，相对路径致 adapter 读不到）；
  - `75d74a1` adapter 禁评论（默认抓评论拖慢采集；评论属期3）。
- **合规口径经用户放宽**（个人自用）：CDP（本机真实浏览器）允许、采集成功率优先；需求/checklist 精简，代理池/CDP/评论不再当红线反复卡。

## 已知项（非阻塞，留作后续）

- MediaCrawler 默认 `CDP_CONNECT_EXISTING=True`，连不上 9222 会**白等 60s 再降级标准浏览器**——能跑但慢；如要去掉 60s 等待，需让 adapter 设其 config（命令行控制不了）。留作可选优化。
- 评论采集 + 导出（按用户「可存」口径，不脱敏）属**期3**。

## 合入

`feat/phase2-mediacrawler` → `main`（`--no-ff`）。

## 蓝图进度

v0：期1（fixture 管线）+ 期2（MediaCrawler 真实采集）已交付；期3（评论，可选）待定。需求保持 `开发中`，开发蓝图待最后一期后归档。
