---
title: 创作者档案落盘 验收记录
type: 验收记录
case: 创作者档案落盘
phase: 1
created: 2026-07-03
---

## 验收结论

**通过**。交付创作者主页档案落盘：fork 让 jsonl 写出创作者档案（原 `store_creator` 空 pass），xhs-recon 解析导出 `creator_profiles.csv`——watchlist 每账号一行官方主页数据。含 A 路线扩展：**官方认证类型 `verify_type`（0未认证/1个人/2机构）+ 小红书号 `red_id`**，机构判定从「昵称/简介人判」升级为**机械可判**（`verify_type == 2` = 官方认证机构号）。旧 11 件导出契约零变化，新增 1 件。

## 验收方式

**实测**（真实采集，用户重新扫码登录后）：测试组 3 账号真实跑 `CONFIG=configs/测试/run.yaml ./run.sh sync`——`创作者档案：3 份`，`creator_profiles.csv` 认证字段实证：路觅 `verify_type=2` red_id=27769215653、辅无忧 `verify_type=2` red_id=63012559526（两机构号 ✓）、张老师 `verify_type=-1`（个人老师号，verifyInfo 无认证字段）。字段名对齐（fork↔parser）动态验证通过，消解首轮唯一盲区。此前 fixture demo、pytest **154 passed**、ruff、preflight 全绿。

## 过程要点

- 分工：Claude 出方案 + 组织评审 + 本人实施；代码评审派独立子代理。
- 标准级阶段（原案）：方案两轮评审通过（#1 需修改 2 阻塞：夹具接线缺环、件数口径错误，v2 关闭）；代码评审通过（1 建议 `_read_creator_profiles` 补 try/except，已修）；Gate 1/2 均用户明示放行。
- **A 路线扩展**（转轻量级，用户「本仓修改全部走轻量级」）：首轮实测发现认证信号不在 tags → 真实探针摸清 `userPageData` 结构 → 定位 `verifyInfo.redOfficialVerifyType` → 双仓补字段 → 真实验收。详见方案页「A 路线扩展」章节。
- 双仓：xhs-recon `feat/creator-profiles`（6 commit）；MediaCrawler fork `4dc9f93`+`5183fb8`（推送用户 fork `darkBaryon/MediaCrawler`）。

## 已知项（非阻塞，留档）

1. **verify_type=-1 vs 0 的语义**：个人号的 `verifyInfo` 里无 `redOfficialVerifyType` 字段（非字段存在且值 0），故降级 -1（未知）而非 0。对判定无影响（`==2` 才是机构），但真实语义如此。
2. **认证判定尚未融入打分**：`verify_type` 仅落 `creator_profiles.csv` 供人判/下游消费，未接入 `account_profile` 打分或 candidate 分层——按需另案（candidate 启发式因认证字段到手，价值大降，可不做）。
3. tags 列保留但实测仅性别图标，信息量低——留档不删（软信号，无害）。

## 合入

xhs-recon `feat/creator-profiles` → `main`（`--no-ff`），推送 origin。MediaCrawler fork 改动已在用户 fork。

## 后续

「专业账号发现」链条至此闭环：search 找账号 → watchlist → sync 拉回**官方认证 + 简介 + 粉丝**，机构判定机械可判。candidate 分层报告因认证字段到手基本无必要。remaining 待办：creator 单会话提速、topic_feed 标 NEW、账号池累积（均按需）。
