---
title: 创作者档案落盘 代码评审 #1
type: 代码评审
case: 创作者档案落盘
phase: 1
round: 1
target: 方案v2 实施（双仓：xhs-recon feat/creator-profiles ab29b65..HEAD；MediaCrawler fork 4dc9f93）
verdict: 通过
created: 2026-07-03
---

## 结论

**通过**（0 阻塞项，2 条建议）。双仓改动均逐行核对完毕：fork 侧确认只动 `store_creator` 一行、模式与 `store_content` 完全一致、未夹带修复既有 `tags` 崩溃路径（自报"不夹带"属实）；xhs-recon 侧最关键的风险点——`CreatorProfile`/`parse_creator_profile` 的 `row.get(...)` key 名与 fork `save_creator` 组装的 `local_db_item` 字段名——逐字核对**完全对齐**（`user_id/nickname/desc/fans/follows/interaction/tag_list/ip_location` 九项无一遗漏无一错位），真实数据字段能否解析出来这一最大风险已通过静态源码交叉核验排除（因偏差1 真实落盘未完成，本次核验是本案数据正确性的唯一证据来源，见下文说明）。软信号降级路径、导出契约（12 件、旧 11 件字节不变、tags 连接格式）均亲核落地。发现 1 处非阻塞的健壮性缺口（`mediacrawler_adapter._read_creator_profiles` 缺 try/except，与同函数内 `_read_creator_results` 的防御不对称），记入建议栏。

---

## 验证记录（独立复跑）

| 命令 | 结果 |
|---|---|
| `uv run ruff check .` | All checks passed!（exit 0）|
| `uv run ruff format --check .` | 42 files already formatted（exit 0）|
| `uv run pytest -q` | **153 passed**（与实施记录一致，起点 142 + 新增 11）|
| `python3 workbench/tools/preflight.py --config workbench/cases/创作者档案落盘/checklist.yaml --repo .` | **全绿**：命令 5/5、产物 12/12（含 `creator_profiles.csv`）、符号 6/6、禁留 0/0 |
| `git diff --quiet ab29b65 -- src/core/{__init__,aggregator,account_ranker,keyword_expander,note_selector,ports,time_window,watchlist}.py src/pipelines/artifacts.py` | exit 0（no-touch 亲跑）|
| `git -C ../MediaCrawler show 4dc9f93` | 单文件单行 diff，见下 |
| `git -C ../MediaCrawler log --oneline 9e17a5e..4dc9f93` | 仅 1 个 commit（无夹带的中间提交）|
| `git log --oneline ab29b65..HEAD`（xhs-recon）| 5 个 commit：`fe36a06/af6abe9/d0e3c29/5db962b/bf65ae4`，与实施记录一致 |
| `git diff --stat ab29b65..HEAD -- . ':!workbench'` | 15 个代码/测试文件，无方案外文件（无 `data/`、无杂散配置）|
| `cat data/exports/latest/creator_profiles.csv`（demo 实跑产物）| 2 行数据，tags 列 `profession:教育\|info:已认证` 与空值分支均正确渲染 |
| `find .../data/raw -iname "*creator_creators_*"` | 除 `tests/fixtures/` 外**零命中**——独立确认偏差1「真实落盘未完成」属实 |

---

## 逐 commit 对账表

### fork `4dc9f93`：`store_creator` 一行

`git -C ../MediaCrawler show 4dc9f93` 全 diff：

```diff
     async def store_creator(self, creator_item: Dict):
-        pass
+        await self.writer.write_to_jsonl(item_type="creators", item=creator_item)
```

- 亲读 `store/xhs/_store_impl.py`：仅 `XhsJsonlStoreImplement.store_creator`（:115）改动；`XhsCsvStoreImplement.store_creator`（:64）与 `XhsJsonStoreImplement.store_creator`（:92）**仍为 `pass`**，未被顺手补写——与方案 §3「不改 csv/json 分支」逐字一致。✅
- 与 `store_content` 模式比对（同类内 :109）：`await self.writer.write_to_jsonl(item_type="contents", item=content_item)`，同一 writer 实例、同一方法、仅 `item_type` 与变量名不同——**模式完全一致**。✅
- **未夹带修复 `save_creator` 的 `tags` 崩溃路径**：亲读 `store/xhs/__init__.py:184-230`，`for tag in creator.get('tags')` 仍无 `or []` 防御，`tag_list` 为 None 时仍会 `TypeError`——方案 §8 明确登记"不在本案夹带"，实测确认原样保留。✅
- `git -C ../MediaCrawler log --oneline 9e17a5e..4dc9f93` 只有这一个 commit，无中间夹带提交。✅

**与方案零偏差。**

---

### `fe36a06` step 2：`CreatorProfile` + `FetchResult.profiles`

亲读 diff：`src/models.py` 新增 `CreatorProfile`（9 字段 + `collected_at`）与 `FetchResult.profiles: list[CreatorProfile] = []`，字段名/默认值与 §4 逐字一致，仅新增无改动，142 起点测试零改动。✅ 与方案零偏差。

---

### `af6abe9` step 3：`parse_creator_profiles_jsonl_lines`

**关键字段映射核对**（本次评审的核心风险点）：

| CreatorProfile 字段 | `parse_creator_profile` 读取 key | fork `local_db_item` 实际 key（`store/xhs/__init__.py:215-228`）| 对齐 |
|---|---|---|---|
| account_id | `row.get("user_id", "")` | `user_id` | ✅ |
| nickname | `row.get("nickname", "")` | `nickname` | ✅ |
| desc | `row.get("desc") or ""` | `desc` | ✅ |
| fans | `normalize_count(row.get("fans"))` | `fans`（int） | ✅ |
| follows | `normalize_count(row.get("follows"))` | `follows`（int） | ✅ |
| interaction | `normalize_count(row.get("interaction"))` | `interaction`（int） | ✅ |
| tags | `_parse_tag_list(row.get("tag_list"))` | `tag_list`（`json.dumps({tagType: name})`） | ✅ |
| ip_location | `row.get("ip_location") or ""` | `ip_location` | ✅ |

九项（含 collected_at 为调用方传入，非行内字段）逐一比对**无一处 key 名错位**——真实数据落盘后能被正确解析，非静默空值。`_parse_tag_list` 对 `json.loads` 异常、非 dict 结果均降级空 `{}`，与 fork 的 `json.dumps({tag.get('tagType'): tag.get('name') for tag in creator.get('tags')})` 序列化形态（dict 的 JSON 串）匹配。✅

坏行/缺键/坏 tag 四组单测覆盖到位（`tests/test_parsers.py`），亲读夹具 `creator_creators_sample.jsonl` 两行覆盖有标签/无标签分支。与方案零偏差。

---

### `d0e3c29` step 4：双 adapter 读回档案

**软信号降级路径 grep 确认**：
- `mediacrawler_adapter.py`：`_read_creator_profiles` 用 `glob("xhs/jsonl/creator_creators_*.jsonl")`，无匹配文件时 `files=[]` → `lines=[]` → parser 返回 `[]`，**不抛异常不入 `failed_ids`**；`profiles.extend(...)` 位于成功分支（笔记已解析成功之后），不影响该账号 `error` 归类。✅
- `fixture_adapter.py`：`_creator_profiles_path is None` 时直接 `return []`；配了路径但 `OSError`（如缺文件）同样 `return []`。`_build_adapter` 仅 fixture 主分支消费 `creator_profiles_fixture_path`，mediacrawler 主分支与降级分支均不消费——亲读 `run_research.py:38-66` 确认与自报口径一致（降级分支走 `FixtureAdapter(fixture_path, comments_path=...)`，未传 `creator_profiles_path`）。✅
- **发现的非阻塞缺口**：`_read_creator_profiles`（mediacrawler 侧）的 `f.read_text(...)` **不在任何 try/except 内**，与同函数内 `_read_creator_results` 被 `except (OSError, ValueError)` 包裹的防御不对称——若 glob 命中文件但读取时发生 `OSError`/`UnicodeDecodeError`（如权限变化、并发写入截断等边缘场景），异常会穿透整个 `for account_id in account_ids` 循环，导致**全部账号**的 `fetch_creator_notes` 崩溃，而非按账号隔离降级。方案 §4 只承诺"缺文件→空列表"这一种情形，字面上未违反，但与 §8"软信号口径保证主管线不受影响"的设计意图有张力。当前测试仅覆盖"文件不存在"（`glob` 为空），未覆盖"文件存在但读取失败"。**记入建议栏**（非阻塞：触发条件窄、不违反方案字面承诺）。

四单测（mediacrawler：命中/缺文件；fixture：命中/None 降级）覆盖到位。与方案基本零偏差（1 条建议级发现）。

---

### `5db962b` step 5：导出 + 全链接线

- `creator_profiles.csv` 8 列（`account_id,nickname,fans,follows,interaction,tags,desc,ip_location`）与 §4 列序逐字一致；`tags` 列用 `_join([f"{k}:{v}" for k,v in p.tags.items()])`，`_join = PIPE.join`（`PIPE="|"`），与既有 `n.tags`/`n.source_keywords` 复用同一 `_join`——格式一致性确认。✅ 亲读 demo 实跑产物：`profession:教育|info:已认证` 与空值行渲染正确。
- **旧 11 件字节不变——机制核实**（非仅信任预飞）：
  - `test_cli_sync_backfills_latest_run_dir`（既有，本 commit 追加 `creator_profiles.csv` 到 `added` 集合断言）：`before`=search 产出的 5 件（accounts/notes/account_rank/typical_notes/report_input），`after`=sync 追加 6 件（含 `creator_profiles.csv`），`for name in before: assert after[name]==before[name]` 锁定前 5 件字节不变。
  - `test_cli_comments_backfills_report_and_keeps_others`（未改动，但覆盖范围因本案自动扩大）：`before`=search+sync 后的 **11 件**（5 件搜索侧 + watchlist/creator_notes/account_profile/topic_feed.md/topic_feed.jsonl + 本案新增 `creator_profiles.csv`），comments 命令后 `changed == {"report_input.md"}`——**间接锁定了 creator_profiles.csv 在 comments 阶段字节不变**，且该断言逻辑本身未被本案改动（自动覆盖，非专门补测）。
  - `test_full_chain_search_sync_comments`（未改动）用 `<=` 子集断言，`creator_profiles.csv` 存在不破坏该测试。
  - 综合：11 旧件的字节不变性由「commit 内新测试（5件）+ 既有测试自动扩大覆盖（10/11 件，含 comments 前）」共同锁定，非仅口头声称。✅
- `export_watch_side` 新增可选参数 `creator_profiles: list[CreatorProfile] | None = None`，`None` 分支不写文件（`test_export_watch_side_no_profiles_omits_file` 覆盖）；`export_all` 透传。✅
- **偏差2 核实（空表头触发机制）**：亲读 `_sync_stage`（`run_research.py:225-226`）——`creator_profiles: list[CreatorProfile] = []` 局部初始化，若 `watchlist_cfg is None` 提前 `return SyncArtifacts()`（此时 `creator_profiles=None`，不产文件）；只要 `watchlist_cfg` 存在（即便 watchlist 为空、或 `fetch_creator_notes` 抛 `NotImplementedError` 被跳过），函数走到 `return SyncArtifacts(..., creator_profiles=creator_profiles)` 时 `creator_profiles` 始终是非 None 的 `[]`（未被真实档案覆盖时保持初始化值）——**确认偏差2 描述准确**：触发机制是"局部变量默认 `[]` 而非导出层的 `None` 判断被绕过"，与 `creator_notes.csv` 既有同款口径一致，非本案引入新的不对称。✅

与方案零偏差；实施记录两条自报偏差均属实、如实登记。

---

## 可证伪质疑清单

### Q1【已关闭】：fork 是否顺手改了 `save_creator` 的 `tags` 崩溃路径（自报"不夹带"是否属实）？

**可证伪点**：若 `store/xhs/__init__.py:225` 的 `for tag in creator.get('tags')` 被加了 `or []` 防御，说明夹带修复未登记。

**验证**：`git -C ../MediaCrawler show 4dc9f93` 全 diff 仅 1 个文件 1 行，`store/xhs/__init__.py` 不在 diff 范围内；`Read` 该文件当前内容确认 `:199`（`creator.get('interactions')`，非本案相关的另一处既有风险）与 `:225-226`（`tag_list` 组装）均未变。**关闭**：无夹带。

### Q2【已关闭】：`CreatorProfile` 解析用的 `row.get(...)` key 名是否与 fork 真实落盘的字段名对得上？

**可证伪点**：若 key 名不一致（如误写 `creator_id` 而非 `user_id`），真实数据会静默解析成全零/空值，比昵称启发式更差。

**验证**：见上方"关键字段映射核对"表，九项逐一比对全部对齐。**关闭**——但需注明：这是**唯一的验证手段**（静态源码交叉核对），因偏差1（真实落盘未完成）没有真实 jsonl 可供动态验证，Gate 2 真实跑前这仍是纸面正确性，非运行时实证。

### Q3【未关闭，建议级】：`mediacrawler_adapter._read_creator_profiles` 的文件读取异常是否被妥善捕获？

**可证伪点**：若 `f.read_text()` 在文件存在但读取失败时抛出未捕获异常，会导致整个 `fetch_creator_notes` 循环崩溃而非按账号降级，与 §8 软信号设计意图相悖。

**验证**：亲读 `mediacrawler_adapter.py:206-212`，`_read_creator_profiles` 无 try/except 包裹，而同函数内 `_read_creator_results`（笔记读取）在调用处有 `except (OSError, ValueError)`。测试套件仅覆盖"文件不存在"（glob 返回空列表，天然不触发读取），未构造"文件存在但读取失败"场景。**未关闭**——触发概率低（需要文件在 glob 后、read 前被并发修改/权限变化），且不违反方案字面承诺（方案只保证"缺文件"softly），故不判阻塞，记入建议栏。

### Q4【已关闭】：偏差1（真实落盘未验证）是否如实，是否存在被掩盖的失败？

**可证伪点**：若 `data/raw` 下实际存在本次实施期间落盘的 `creator_creators_*.jsonl`（说明验证其实做了但结果不利、未如实报告），则偏差记录不实。

**验证**：`find .../data/raw -iname "*creator_creators_*"` 除 `tests/fixtures/` 外零命中；`find .../MediaCrawler -iname "*creator*" -newer store/xhs/_store_impl.py` 也无新落盘证据。已有的真实 creator 目录（`data/raw/2026-07-02T*/creator/*/xhs/jsonl/`）只有 `creator_contents_*.jsonl`（笔记），无 `creator_creators_*.jsonl`（档案）——与"fork 补丁前跑的、登录过期后没有新跑"的时间线吻合。**关闭**：偏差1 属实，无掩盖迹象。

---

## 建议栏（非阻塞）

1. **`mediacrawler_adapter._read_creator_profiles` 补 try/except**（Q3）：与同函数内 `_read_creator_results` 保持一致的防御（`except (OSError, UnicodeDecodeError)` → 该账号档案降级为空，不影响笔记结果），避免罕见 IO 异常放大为全账号崩溃；顺带补一条"文件存在但读取失败"的单测覆盖该分支。
2. **Gate 2 真实跑时优先验证字段映射的动态正确性**：本轮评审的字段名核对（Q2）是纯静态代码比对，真实小红书 `userPageData` 返回的 `tags`/`interactions` 结构是否与 `save_creator` 假设的形态一致（尤其 `tag_list` 里认证类 `tagType` 具体取值）仍未经真实数据验证——方案 §8 已自陈此风险，建议 Gate 2 验收时把「至少 1 个机构号 `tags` 含预期认证类 key」列为明确检查项，而不仅是"粉丝量级对上即可"。

---

## 验证记录汇总

1. 独立复跑 lint/format/pytest/preflight：全绿（153 passed；preflight 命令 5/5、产物 12/12、符号 6/6、禁留 0/0）。
2. fork 仓 `git show 4dc9f93` 亲核：单文件单行，与 `store_content` 模式一致，未夹带 `tags` 崩溃路径修复。
3. 关键字段映射表逐项核对：`CreatorProfile` 解析 key 与 fork `local_db_item` key 完全对齐（本案数据正确性最大风险点，已用静态源码交叉验证排除，非运行时实证）。
4. 软信号降级路径 grep+读码确认：两处 adapter 缺文件均不入 `error`/`failed_ids`；发现 1 处（mediacrawler 侧档案读取无 try/except）非阻塞健壮性缺口，记入建议栏。
5. 导出契约核实：`creator_profiles.csv` 列头/tags 连接格式与既有 `_join` 一致；旧 11 件字节不变由新测试+既有测试覆盖扩大共同锁定（非仅口头声称），机制逐层核对。
6. 偏差1（真实落盘未验证）：`find` 全仓核实零命中真实档案 jsonl，与"登录过期未重跑"时间线吻合，判定属实无掩盖。
7. 偏差2（空表头触发机制）：亲读 `_sync_stage` 局部变量初始化逻辑，确认"非 None 默认 `[]`"是触发根因，描述准确。
8. 夹带扫描：`git diff --stat ab29b65..HEAD -- . ':!workbench'` 15 文件全部落在方案 §4 白名单内，无方案外改动；fork 侧 `git log 9e17a5e..4dc9f93` 仅 1 commit 无中间夹带。
