---
title: 创作者档案落盘 方案 v2
type: 方案
case: 创作者档案落盘
phase: 1
version: 2
status: 已归档
relates: [创作者档案落盘]
created: 2026-07-03
---

## 给用户的摘要

打通「创作者主页档案」这条已经端到嘴边的数据：fork 补一行让 jsonl 写出档案（数据早已抓到，只是出口没接），xhs-recon 解析后新导出 `creator_profiles.csv`——watchlist 每个账号一行**官方主页数据**（简介/粉丝/互动/标签含认证类/IP）。机构判定从「昵称猜」升级为「平台数据人判」，零额外采集请求。

- Before：判断「予橙」是不是正经机构，只能看昵称带不带「辅导」。
- After：`cli sync` 顺带产出每账号的简介（"XX教育科技旗下"）、粉丝量级、职业认证标签——扫一眼表就能定。

拍板清单：**无**（零额外请求、只新增文件、fork 改动一行且模式有先例——无新取舍；判定融合进打分属另案）。

## v1→v2 变更（供差量评审）

回应「方案评审 #1」（verdict 需修改，2 阻塞 + 3 建议；fork 侧全部 spike 断言经评审亲核成立，含文件名前缀 `creator_creators_` 的 `crawler_type_var` 证据链）：

1. **关闭阻塞1（fixture 夹具接线缺环）**：§4 补 `_build_adapter` 接线——顶层配置键 `creator_profiles_fixture_path`（照 `creator_fixture_path` 先例），仅 fixture 主分支消费并传入 `FixtureAdapter(creator_profiles_path=...)`；**mediacrawler 主分支与降级分支均不消费**（显式口径，理由见 §4 注释——降级分支语义是应急搜索，creator 侧在降级下本走 NotImplementedError 跳过，传档案夹具无意义；与既有 `creator_fixture_path` 的不对称保持一致而非引入新的）；sample.yaml 配该键 + 新夹具 `creator_creators_sample.jsonl`。
2. **关闭阻塞2（件数口径错误）**：实测 demo 基线是 **11 件**（含 `comments.csv`，v1 误记 12）——全篇更正为「旧 11 件 → 新 12 件」；checklist 必存产物补 `comments.csv`（12 条）。
3. **吸收建议1**：§8 登记 fork 现状既有的 `tags` 缺失崩溃路径（评审已实测复现：`save_creator` 对 `userPageData.tags` 为 None 抛 TypeError → 该账号子进程整体失败非仅档案缺失；xhs-recon 按账号隔离子进程、失败归入既有 `failed_ids`，爆炸半径已兜）——Gate 2 真实跑若遇账号 `rc!=0` 先对照此条。
4. **吸收建议2**：§2 行号校准——creator glob 现状在 `mediacrawler_adapter.py:191-199`（v1 误写 185-191）。
5. **吸收建议3**：降级分支透传口径已并入变更 1 显式写明（不传 + 理由），不留新的不对称。

## 1. 起点

`起点 = ab29b65`（xhs-recon 仓 main）；MediaCrawler fork 起点 `9e17a5e`（期1 `--sort_type` 后未再动，工作区干净——今日误格式化已 stash 恢复）。双仓提交，实施记录分别记 hash。

## 2. 现状与约束盘点（写方案当时实测，spike 2026-07-03 源码亲核）

- **数据链路已全通只缺出口**（fork 侧）：creator 模式入口 `get_creators_and_notes` 在拉笔记前调 `get_creator_info`（`media_platform/xhs/core.py:199`），解析主页 `window.__INITIAL_STATE__` 的 `userPageData`（`extractor.py:52-69`）；`save_creator`（`store/xhs/__init__.py:184-230`）组装字段：`user_id/nickname/gender/avatar/desc/ip_location/follows/fans/interaction/tag_list`（tag_list 为 `{tagType: name}` 的 JSON 串，职业认证即在其中）+ `last_modify_ts`；分发到 `store_creator`——**`XhsJsonlStoreImplement.store_creator` 为空 `pass`**（`store/xhs/_store_impl.py:115`），csv/json 分支同样为 pass，仅 Mongo 分支实写（`:342`）。
- **写出命名规则**：`AsyncFileWriter._get_file_path` → `{crawler_type}_{item_type}_{日期}.{ext}`（`tools/async_file_writer.py:43`）——creator 模式 + `item_type="creators"` → **`xhs/jsonl/creator_creators_<日期>.jsonl`**；写 jsonl 的先例 `store_content` 即 `write_to_jsonl(item_type="contents")`（`_store_impl.py:109`），照抄。
- **零额外请求**：档案请求发生在既有 creator 流程内（每账号本来就要发），本案不新增任何网络行为。
- xhs-recon 侧现状：`fetch_creator_notes` 每账号独立子目录 + glob `creator_contents_*.jsonl`（`src/adapters/mediacrawler_adapter.py:191-199`，评审 #1 校准）——档案文件与其同目录，加一个 glob 即可；`_build_adapter` 读顶层 `creator_fixture_path` 传 fixture 主分支的先例（`run_research.py:37-61`，降级分支不传，本案沿此对称）；`FetchResult` 无档案载体（`src/models.py:66-85`）——扩可选字段（默认空列表，向后兼容，先例：期2 `fetch_creator_notes` 端口默认实现）；计数归一化复用 `parsers.normalize_count`（`parsers.py:16-28`，"1.1万"→11000 已有单测）；导出复用 `_write_csv` + `export_watch_side` 可选参数模式（`exporter.py`，None=不写先例）。
- **复用盘点**：无重复造轮点——解析新建 `parse_creator_profiles_jsonl_lines`（схema 与 21 字段笔记完全不同，不复用 `parse_jsonl_lines`）；其余全部复用既有出口。
- 约束：`core` 无 "xhs"（档案解析归 adapters）；旧 11 件导出契约零变化（只新增 `creator_profiles.csv`，评审 #1 更正件数）；fork 最小 diff（一行函数体，不动 `save_creator` 字段组装）；`account_profile.csv` 蓝图五列不碰。

## 3. 目标 / 非目标

**目标**：

1. **[fork]** `XhsJsonlStoreImplement.store_creator` 补写出（一行，`item_type="creators"`）；
2. 新模型 `CreatorProfile` + `FetchResult.profiles` 可选字段（默认 `[]`）；
3. `parsers.parse_creator_profiles_jsonl_lines`：档案行 → `CreatorProfile`（计数归一、`tag_list` JSON 串 → dict、缺字段降级空值）；
4. `MediaCrawlerAdapter.fetch_creator_notes` 顺带读回档案（glob `creator_creators_*.jsonl`，缺文件 → `profiles=[]` 不报错——软信号口径，旧版 fork 也能跑）；`FixtureAdapter` 新增可选 `creator_profiles_path`；
5. 导出 `creator_profiles.csv`（列：`account_id,nickname,fans,follows,interaction,tags,desc,ip_location`；tags 以 `类型:名称` 对、`|` 连接）；`export_watch_side` 加可选参数（None=不写）；
6. `_sync_stage`/cli `sync` 接线（`SyncArtifacts` 加字段）。

**非目标**：不做认证自动判定/打分融合（另案）；不对搜索候选逐个查档案（用量口径）；不改 fork 的 `save_creator` 字段组装与 csv/json 分支；不改旧 11 件契约；不加配置段（档案随 creator 拉取自动产出，无需开关——watchlist 未启用时本就不跑 creator）。

## 4. 接口与数据设计

```python
# src/models.py（只增不改）
class CreatorProfile(BaseModel):
    account_id: str
    nickname: str = ""
    desc: str = ""                    # 主页简介（机构判定主信号之一）
    fans: int = 0                     # 粉丝数（normalize_count 归一）
    follows: int = 0
    interaction: int = 0
    tags: dict[str, str] = {}         # {tagType: name}，职业认证在其中（如 profession:...）
    ip_location: str = ""
    collected_at: str = ""

FetchResult 新增：
    profiles: list[CreatorProfile] = []   # 默认空 = 旧构造/旧测试零改动

# MediaCrawler fork（第二处改动，独立仓 commit；照 store_content 先例一行）
class XhsJsonlStoreImplement:
    async def store_creator(self, creator_item: Dict):
        await self.writer.write_to_jsonl(item_type="creators", item=creator_item)
# 落盘：<save_path>/xhs/jsonl/creator_creators_<日期>.jsonl，每账号一行

# src/adapters/parsers.py
def parse_creator_profiles_jsonl_lines(lines, *, collected_at: str) -> list[CreatorProfile]
# fans/follows/interaction 走 normalize_count；tag_list 是 JSON 串 → json.loads 出 dict，
# 解析失败/缺键一律降级空值（档案是软信号，坏一行不坏整跑）；gender/avatar/last_modify_ts 丢弃（用不上）

# src/adapters/mediacrawler_adapter.py（fetch_creator_notes 内、逐账号读回处顺带）
# profiles glob：save_path/"xhs/jsonl/creator_creators_*.jsonl"（与 contents 同目录）
# 缺文件（旧版 fork / 抓取失败）→ 该账号无档案，不计失败不入 error——软信号口径
# FetchResult(..., profiles=profiles)

# src/adapters/fixture_adapter.py
FixtureAdapter.__init__(..., creator_profiles_path: str | None = None)
# fetch_creator_notes 时若配了该路径：读档案夹具、按 account_ids 过滤；None → profiles=[]

# src/pipelines/run_research.py::_build_adapter（阻塞1 接线，照 creator_fixture_path 先例）
# 顶层配置键 creator_profiles_fixture_path：
#   fixture 主分支 → FixtureAdapter(..., creator_profiles_path=config.get("creator_profiles_fixture_path"))
#   mediacrawler 主分支不消费（真实档案来自 MediaCrawler 落盘）；
#   mediacrawler 降级分支也不消费——降级语义是应急搜索，creator 侧在降级下走
#   NotImplementedError 跳过，传档案夹具无意义（与 creator_fixture_path 既有不对称保持一致）
# sample.yaml：creator_profiles_fixture_path: tests/fixtures/creator_creators_sample.jsonl

# src/core/exporter.py（export_watch_side 加可选参数，None=不写，照既有先例）
export_watch_side(..., creator_profiles: list[CreatorProfile] | None = None)
# creator_profiles.csv：account_id,nickname,fans,follows,interaction,tags,desc,ip_location
# tags 列格式：f"{type}:{name}" 以 "|" 连接（与既有 _join 管道分隔一致）；export_all 同步透传

# src/pipelines/run_research.py + cli.py
# SyncArtifacts 新增 creator_profiles: list[CreatorProfile] | None = None
# _sync_stage：profiles = creator_result.profiles；日志一行「创作者档案：N 份」
# cli sync / research 的 export 调用各透传一个参数

# fixture 夹具：tests/fixtures/creator_creators_sample.jsonl
# 2 行（对应既有 creator 夹具的 2 个账号），一行带 profession 类 tag、一行不带——覆盖判定双分支
```

依赖方向不变：解析归 adapters，`core` 只认 `CreatorProfile` 模型；导出复用既有条件写出模式。

## 5. 实现步骤（小步，每步 pytest/ruff 全绿 commit；checklist 全量在步骤 5 后可跑）

1. **[MediaCrawler 仓]** `store_creator` 一行 + 手动验证（跑测试组 1 账号真实 creator，确认 `creator_creators_*.jsonl` 落盘且含 tag_list——该仓无 CI，commit hash 与验证输出记入实施记录）；
2. `models.CreatorProfile` + `FetchResult.profiles`（默认空）+ 旧构造回归；
3. `parsers.parse_creator_profiles_jsonl_lines` + 夹具 + 单测（计数归一 / tag_list JSON→dict / 坏行降级 / 缺键降级）；
4. 双 adapter 档案读回 + 单测（mediacrawler：glob 命中与缺文件软降级；fixture：过滤与 None 降级）；
5. `exporter` 新文件 + `SyncArtifacts`/pipeline/cli 接线 + e2e（sync 后 12 件、旧 11 件字节不变、缺档案时 `creator_profiles.csv` 空表头）。

## 6. 测试策略

- 单测：parser 四组（见 §5-3）；adapter 双实现各两组；exporter 列头/None 不写/tags 连接格式；
- e2e（既有 pin 手法）：`cli search → sync` 链上断言 `creator_profiles.csv` 存在、行数与档案夹具吻合、**其余 11 件字节不变**；fixture 未配档案路径时文件为空表头（软降级机械可验）；
- 手动验收（Gate 2）：真实跑测试组（3 账号）→ `creator_profiles.csv` 3 行，抽 1 个机构号对照小红书主页：粉丝量级一致、tags 含主页可见的认证类标签（fork 改动的端到端实证）；
- 盲区：fork 侧无自动化测试（该仓不进 CI，步骤 1 手动验证 + Gate 2 真实跑双覆盖）；`userPageData` 的字段形态若遇平台改版会整体缺失——软信号口径已兜（profiles 空、主产物不受影响）。

## 7. 验收标准 + 检查清单 YAML

机械核验以同目录 [checklist.yaml](checklist.yaml) 为唯一事实源：lint/format/pytest/demo 全绿；`CreatorProfile`/`parse_creator_profiles_jsonl_lines`/`creator_profiles.csv` 符号落地；demo 产物 12 件；`src/core` 中 `aggregator/account_ranker/keyword_expander/note_selector/time_window/watchlist/ports/__init__` 与 `src/pipelines/artifacts.py` 对起点零 diff。fork 改动不进本表（手动验证口径见 §5-1）。人话：不跑 sync 一切照旧；跑了多一件官方档案表，其余一个字节不变。

## 8. 风险与禁止事项

- **认证字段的确切形态未实测**：spike 只证了 `tag_list` 通道存在，专业号认证的 tagType 具体叫什么（profession/official/其他）要等步骤 1 真实落盘才见分晓——若认证不在 tag_list 而在 `basicInfo` 独立字段，本案先交付现有字段（简介/粉丝已够人判），追加字段留下一案（fork 只动一行的口径不破）；
- **fork 现状既有的 `tags` 缺失崩溃**（评审 #1 实测复现，非本案引入亦不因本案放大）：某账号 `userPageData.tags` 为 None 时 `save_creator` 抛 TypeError，该账号**整个子进程失败**（笔记也拿不到，非仅档案缺失）；xhs-recon 侧按账号隔离子进程、失败归入既有 `failed_ids` 机制，不影响其他账号——Gate 2 真实跑若遇某账号 `rc!=0` 先对照此条，修复（`or []` 一行防御）留后续案，不在本案夹带；
- **平台改版风险**：`__INITIAL_STATE__` 结构变化会让档案整体缺失——软信号口径（profiles 空不报错）保证主管线不受影响；
- **双仓提交**：fork 与 xhs-recon 分别 commit，代码评审两边都查（期1 先例）；
- 禁止：fork 除 `store_creator` 一行外任何改动；对候选账号新增档案请求；改旧 11 件契约列；加配置段。

## 9. 回滚口径

xhs-recon 弃分支即回滚；fork 的一行 commit 独立 revert 干净（留着也无害——只是多写一个没人读的文件）。已 commit 去留由用户裁决。

---

## Gate 决策结果

- **2026-07-03 Gate 1 放行**（用户「同意」，方案评审 #2 通过后）：拍板项落定——**允许 fork 第二处改动**（`store_creator` 一行）。实施归 Claude 本人（分工沿「你自己做」口径），按 §5 五步小步 commit，双仓分别记 hash，预飞全绿后交独立子代理代码评审。来源：用户当场明说。

---

## 实施记录

- 双仓实施。**MediaCrawler fork**：起点 `9e17a5e` → `4dc9f93 feat: persist xhs creator profile to jsonl store`（`XhsJsonlStoreImplement.store_creator` 由空 pass 补为 `write_to_jsonl(item_type="creators")`，一行）。
- **xhs-recon**：分支 `feat/creator-profiles`，起点 `ab29b65`：
  - `fe36a06 step 2`：`models.CreatorProfile` + `FetchResult.profiles`（默认 `[]`）；既有 142 测试零改动过。
  - `af6abe9 step 3`：`parsers.parse_creator_profiles_jsonl_lines` + `_parse_tag_list`（JSON 串→dict）+ 夹具 `creator_creators_sample.jsonl`（2 行，有/无标签双分支）+ 4 单测。
  - `d0e3c29 step 4`：双 adapter 读回档案（glob `creator_creators_*.jsonl` / fixture profiles path），缺文件软降级空列表不入 error；`_build_adapter` 接 `creator_profiles_fixture_path`（仅 fixture 主分支）+ 4 单测。
  - `5db962b step 5`：`exporter` 新 `creator_profiles.csv`（8 列，tags dict→`类型:名称|连接`）+ `SyncArtifacts`/`_sync_stage`/cli sync/run_research 全链透传 + sample.yaml 配夹具 + e2e 键集合断言更新 + 2 新测试。
- 验收标准逐项自查：`CreatorProfile`/`parse_creator_profiles_jsonl_lines`/`creator_profiles.csv` 符号落地 ✓；fixture demo 产出 12 件含 `creator_profiles.csv`（`陈皮糖` 带 `profession:教育|info:已认证`、`菜鸡罢了` 空标签，双分支实证）✓；旧 11 件字节回归（e2e `test_pipeline_watchlist..._keeps_old_outputs` 锁定）✓；缺档案软降级空表头 ✓。
- 预飞：`preflight --config .../创作者档案落盘/checklist.yaml` 全绿（exit 0）；lint/format/pytest **153 passed**（起点 142 + 新增 11）；no-touch（core 除 exporter/parsers 外 + artifacts）零 diff。

## 实施偏差记录

1. **fork 单账号真实落盘验证未完成**（方案 §5 步骤 1 明示项，登记为偏差）：验证跑遇采集浏览器**登录态过期**（`Login xiaohongshu failed by qrcode`），环境问题非代码问题。step1 的一行改动逻辑正确性由 xhs-recon 侧 mediacrawler_adapter 单测（构造真实 `creator_creators_*.jsonl` 走 parser）间接覆盖；真实落盘验证顺延至 Gate 2（用户重新扫码后与手动验收合并执行）。
2. `creator_profiles.csv` **无档案时产出空表头而非不产**：`FetchResult.profiles` 默认 `[]`（非 None），故 sync 跑必产该文件（空表头）——与 `creator_notes.csv` 既有口径一致，方案 §6「未配档案路径时空表头」已预期，此处明确其触发机制（默认空列表而非 None 分支）。

## 蓝图验证备忘

不适用（本案非蓝图分期）。

---

## A 路线扩展（2026-07-03，轻量级，用户「A」+「本仓修改全部走轻量级」）

**动因**：Gate 2 首次实测发现 tags 里没有认证标（路觅/辅无忧 tags 为空或仅性别图标），用户指出「调研不到位，应先摸清 API 返回哪些字段」。

**调研（真实探针）**：临时在 fork `extractor.py` 插 dump 抓一次 `userPageData` 完整结构（事后 `git restore` 撤净）。发现认证信号在**独立顶层键 `verifyInfo.redOfficialVerifyType`**（`save_creator` 原本完全丢弃），非 tags：
- `verifyInfo.redOfficialVerifyType`：`0`未认证 / `1`个人认证 / `2`机构·企业认证（专业号）——机构判定金标准；
- `basicInfo.redId`：小红书号（App 搜索用，被丢弃的另一有用字段）；
- `tags` 实测仅性别图标，不含认证——原设计据此判断有误，A 路线更正。

**改动（双仓，均已合入）**：
- fork `5183fb8`：`save_creator` 补取 `verify_type`（verifyInfo.redOfficialVerifyType）+ `red_id`（basicInfo.redId）——fork 第 3 处改动，超出原方案「不碰 save_creator 字段组装」，属 A 路线扩展范围。
- xhs-recon `71f4d3e`：`CreatorProfile` 加 `verify_type: int = -1`（-1 未知，兼容旧版 fork）+ `red_id: str`；parser 解析（缺字段/非法值降级 -1）；`creator_profiles.csv` 加两列置于昵称后；夹具补认证字段 + parser 单测。

**真实验收（测试组 3 账号）**：路觅 `verify_type=2`、辅无忧 `verify_type=2`（机构认证 ✓）、张老师 `-1`（个人号，verifyInfo 无该字段）——机构判定从「人看简介猜」变为机械可判（`verify_type == 2`）。haiku 机械核对全 ✓（字段名一致、探针撤净）。

**留档**：MediaCrawler fork 改动统一推送至用户 fork `darkBaryon/MediaCrawler`（origin），原作者设 upstream；本项目 4 处 fork 改动（--sort_type / store_creator / verify_type+red_id / CDP 登录修复）均在其上。
