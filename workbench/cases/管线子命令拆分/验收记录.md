---
title: 管线子命令拆分 验收记录
type: 验收记录
case: 管线子命令拆分
created: 2026-07-03
---

## 验收结论

**通过**。交付四个 typer 子命令：`search`（广角，建运行目录）/ `sync`（长焦，补全写回 latest 目录）/ `comments`（深读，补全写回）/ `research`（组合=旧行为）。命令间靠 `data/exports/latest/` 文件契约衔接（`artifacts.py` 读回，空 `|` 列坑已锁定）；旧入口 `run_research` 与旧 11 件导出契约逐字节不变（等价性 e2e 机械证明）；exporter 仅提取式重构出两个子集出口。

## 验收方式

**抽查**（Gate 2 抽查档）：用户本人跑 `cli search`（fixture）；机械复核含三命令链临时目录实跑（search→sync→comments 全通、11 件齐活）。pytest **135 passed**（起点 117 + 新增 18）、ruff、预飞全绿，均由代码评审独立复跑核实。

## 过程要点

- 分工：Claude 出方案 + 组织评审 + **本人实施**（用户「你自己做就好」，2026-07-03 分工变更）；代码评审仍派独立子代理（作者=实施者，第二双眼睛不省——本案证明必要，见下）。
- 方案评审两轮：#1 需修改（3 阻塞：sync 空表头覆盖 latest 自断链路 → v2 改补全式写回；`"".split("|")==['']` 反序列化形变；`_now_iso` patch 口径）；#2 通过。
- 代码评审两轮：#1 需修改（阻塞：`data/exports_real/` 5 个 git 跟踪文件的删除混入 step1「纯重构」commit 且未登记——删除有用户清空指令出处，但漏报属实，补登偏差 5）；#2 通过。**独立评审抓到了实施者自己看不见的问题，自实施+自评审模式的必要性实证。**
- Gate 2 拍板：`exports_real` **维持删除**（v0 遗留、git 历史 `e9640d1` 可随时恢复；废纸篓副本已因废纸篓清空不存在）。

## 已知项（非阻塞，留档）

1. **`cli comments` 尊重 `comments.enabled` 开关**（实施偏差 4）：显式调用也会被 `enabled: false` 拦下，反直觉——已有现行为回归锁，改为「显式调用视为启用」待下一案带上（用户未裁决，倾向改）。
2. 私有函数跨模块调用（cli.py 依赖 run_research 8 个下划线函数）与榜单空表头无警告——代码评审建议 1/3 留档。
3. sync 的 auto 名额基于最近一次 search 榜单（可能滞后），属预期语义，README 已写明。
4. 步骤 4/5 提交粒度合并、exporter 写出时序变化——实施偏差 1/2，均无契约影响。

## 合入

`feat/cli-subcommands` → `main`（`--no-ff`），推送 origin。

## 后续

下一案（修复与优化批次，档位待定）：B1 search 落盘隔离、B2 search 读回包 try、B5/B6 隐患、评论批量上限、creator 单会话 spike、`cli comments` 开关语义变更。
