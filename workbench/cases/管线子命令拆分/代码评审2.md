---
title: 管线子命令拆分 代码评审 #2
type: 代码评审
case: 管线子命令拆分
round: 2
target: 修复 commit 1e5d5ff（评审 #1 阻塞1 + 建议2/4 的响应）
verdict: 通过
created: 2026-07-03
---

## 结论

**通过**。评审 #1 的唯一阻塞项（`data/exports_real/` 5 文件 797 行被顺手删除且未登记）已按规范允许的方式关闭——**登记 + 用户裁决**，不要求恢复文件或改写历史（对照《评审规则》「范围哲学」：偏差处置是「登记，由用户裁决去留」，不是「必须撤销」）。偏差5 的四要素（溯源/卷入机制/可恢复性/裁决口径）齐全且与评审 #1 认定的事实（5 文件、797 行、`b2837ba`）逐字对应，无缩写、无回避。建议2/4 的测试补充亲跑确认真锁定了对应行为（非目录 latest 抛 `ValueError` 且消息命中 `resolved.is_dir()` 分支；`enabled=false` 时 `_comments_stage` 返回 `[]`、`export_comments` 因此不写 `comments.csv`）。建议1/3 留档不改在 commit message 里有交代。夹带扫描：`1e5d5ff` 只动 4 个文件（`tests/test_artifacts.py`、`tests/test_cli.py`、新增的`代码评审1.md`存档、`方案v2.md` +1 行），无代码逻辑改动。全量回归 135 passed（133+2），preflight 22 项全绿。

**唯一需要用户注意但不构成阻塞的发现**：偏差5 登记的「可恢复性」两个来源之一——废纸篓路径 `~/.Trash/xhs-recon-data-1783022861`——亲核当前环境下**不存在**（`~/.Trash` 目录为空，0 个条目）。这不影响本轮评审关闭，因为（a）另一个来源即 git 历史（`e9640d1`）经亲核确凿可恢复，登记的核心承诺「数据未真丢」并未落空；（b）废纸篓是操作系统级易变状态，可能在登记之后被用户或系统清空，这属于登记时点的真实快照而非登记内容造假。计入质疑清单，供 Gate 2 裁决时一并知悉（若用户想凭废纸篓恢复，需先确认该路径当下是否还在）。

---

## 逐条对账

### 1. 阻塞是否关闭：偏差5 登记完整性

亲核 `方案v2.md` 「实施偏差记录」第5条原文：

> **`data/exports_real/` 5 个 git 跟踪文件的删除被卷入 step1 commit**（代码评审 #1 阻塞，漏报补登）：2026-07-03 用户指令「清空 data」时该遗留目录被移入废纸篓（`~/.Trash/xhs-recon-data-1783022861`），其为 git 跟踪文件，工作区删除被 step1 的 `git add -A` 一并提交进 `b2837ba`——删除有用户授权出处，但混入「纯重构」commit 且未登记属实。数据双份可恢复（git 历史 `e9640d1` + 废纸篓）。去留由用户 Gate 2 裁决：维持删除（推荐，属清理指令的延续）或 `git checkout e9640d1 -- data/exports_real` 恢复。

四要素逐条核对：

| 要素 | 登记原文 | 亲核结论 |
|---|---|---|
| 溯源（用户清空指令） | 「2026-07-03 用户指令「清空 data」时该遗留目录被移入废纸篓」 | 有明确出处描述（用户指令 + 时间），✅ 齐 |
| 卷入机制（`git add -A`） | 「工作区删除被 step1 的 `git add -A` 一并提交进 `b2837ba`」 | 与评审 #1 认定的机制一致，`git show b2837ba --stat` 亲核确实是该 commit 一并删除 5 文件，✅ 齐 |
| 可恢复性（git 历史 + 废纸篓路径） | 「数据双份可恢复（git 历史 `e9640d1` + 废纸篓）」+ 具体废纸篓路径 + `git checkout e9640d1 -- data/exports_real` 命令 | git 历史侧亲核确凿（见下）；废纸篓侧路径给出但当前环境不存在，见下方质疑 Q1。✅ 形式齐全，⚠️ 内容部分不可复核 |
| 裁决口径（Gate 2 用户定去留） | 「去留由用户 Gate 2 裁决：维持删除（推荐）或恢复」 | 明确写出两个选项+推荐倾向，未擅自定夺，✅ 齐 |

**与评审 #1 认定事实的一致性**：亲核 `git show e9640d1 --stat -- data/exports_real/` 确认 5 个文件（`account_rank.csv`/`accounts.csv`/`notes.csv`/`report_input.md`/`typical_notes.csv`），`git show --stat b2837ba` 显示 `5 files changed, 797 deletions(-)`——与评审 #1「5 文件 797 行」逐字对应，无缩水无夸大。

**关闭方式合规性判定**：对照《评审规则》「范围哲学」——「实施只能做方案列明的；顺手多做的……= 偏差，必须登记，由用户裁决去留」。规则原文只要求「登记 + 裁决」，未要求「恢复」或「改写历史」。偏差5 已完成登记（四要素齐全）并明确移交 Gate 2 裁决，未擅自替用户决定保留或恢复，**关闭方式合规**。评审 #1 阻塞项的关闭条件是「登记」而非「恢复文件」，`1e5d5ff` 满足该条件。✅ **阻塞关闭**。

---

### 2. 建议2/4 测试补充有效性

亲跑：

```
$ uv run pytest tests/test_artifacts.py tests/test_cli.py -q
18 passed in 0.23s
```

**建议2（`resolve_latest_run_dir` 非目录分支）**：新测试 `test_resolve_latest_pointing_to_file_raises`（`tests/test_artifacts.py`）构造 `latest` 软链指向一个真实文件（非目录），断言 `pytest.raises(ValueError, match="有效运行目录")`。亲读 `src/pipelines/artifacts.py:77-78`：

```python
if not resolved.is_dir():
    raise ValueError(f"{latest} 未指向有效运行目录：{resolved}")
```

`match="有效运行目录"` 精确命中该分支独有的错误消息文案（「不存在」分支的文案是 `"{latest} 不存在——请先跑..."`，不含「有效运行目录」四字），两个分支消息互斥，测试**真正区分并锁定了目标分支**，非误配走到别的分支也能通过的宽松断言。✅ 有效，评审 #1 质疑 Q2 关闭。

**建议4（`comments.enabled=false` 回归锁）**：新测试 `test_cli_comments_respects_enabled_flag`（`tests/test_cli.py`）用 `_cfg(out, comments=False)` 构造配置，先 `search` 建目录，再显式 `comments`，断言 `"comments.csv" not in names`。亲读实现链路：`cli.comments()`（`src/pipelines/cli.py:147`）调用 `run_research._comments_stage(...)`；`_comments_stage`（`src/pipelines/run_research.py:263`）内 `if comments_cfg.get("enabled"):` 为假时不采集、返回 `[]`；`export_comments`（`src/core/exporter.py:252`）内 `if comments:` 为假时跳过 `comments.csv` 写出（`report_input.md` 仍无条件重写，测试未对此断言但也未误导，测试意图明确写在 docstring「开关拦下，未采集」）。测试断言与代码实际路径一致，**真锁定了该行为**（若未来有人误改 `_comments_stage` 使其忽略 `enabled` 开关直接采集，该测试会失败）。✅ 有效，评审 #1 质疑 Q4 的自动化回归缺口补上。

---

### 3. 建议1/3 留档不改的处置

亲核 `1e5d5ff` commit message 第四行：「建议1（私有函数跨模块）/建议3（空表头无警告）留档不改」。两条建议均在 message 里明确点名并说明处置方式（不改动代码，仅记录），符合《评审规则》建议栏「评审职责不立案，由设计统一立」的定位——建议是否采纳属设计裁量，本轮只需确认「有交代」而非「必须采纳」。✅ 已交代。

---

### 4. 修复 commit 是否夹带

```
$ git show 1e5d5ff --stat --summary
 tests/test_artifacts.py   |   9 +
 tests/test_cli.py         |  12 ++
 .../代码评审1.md           | 227 +++ (新增)
 .../方案v2.md              |   1 +
 4 files changed, 249 insertions(+)
```

四个文件：`tests/test_artifacts.py`（+9，建议2 测试）、`tests/test_cli.py`（+12，建议4 测试）、`workbench/cases/管线子命令拆分/代码评审1.md`（新增227行，即评审 #1 本身归档进案例目录——流程文档，非代码）、`方案v2.md`（+1行，偏差5 登记）。**无任何 `src/` 下的逻辑代码改动**，与要求「应只有 方案v2.md/test_artifacts.py/test_cli.py 三个文件」相比多出的 `代码评审1.md` 是评审记录归档（此前评审 #1 完成后落盘到案例目录的常规操作，非代码逻辑，不构成「夹带」意义上的问题）。✅ 无逻辑夹带。

---

### 5. 全量回归

```
$ uv run pytest -q
135 passed in 0.30s   （133 + 建议2/4 新增 2 条，与预期吻合）

$ python3 workbench/tools/preflight.py --config workbench/cases/管线子命令拆分/checklist.yaml --repo .
全绿：命令 6/6、产物 10/10、符号 8/8、禁留 2/2，exit 0
```

与预期完全一致。

---

## 可证伪质疑清单

### Q1【未关闭，非阻塞，供 Gate 2 知悉】：偏差5 登记的废纸篓路径 `~/.Trash/xhs-recon-data-1783022861` 当前是否真实存在？

**可证伪点**：若该路径不存在，说明「数据双份可恢复」的第二份来源在评审时点已不可用，用户若打算走「废纸篓」路径恢复会扑空（不影响 git 历史那份仍可用）。

**验证**：亲跑 `ls -la ~/.Trash/xhs-recon-data-1783022861` → `exit 1`（不存在）；`ls ~/.Trash | wc -l` → `0`（废纸篓当前完全为空）；`find / -maxdepth 3 -iname ".Trash"` 确认只有 `/Users/xinyue/.Trash` 这一个废纸篓路径，非环境识别错误。**未关闭**——无法证实该路径此刻仍持有数据，但也无法证明登记时该路径不存在（废纸篓内容会随时间被用户清空或系统自动清理，30 天等），故不判定登记造假。**结论**：该项不影响本轮阻塞关闭判定（因为 git 历史侧的可恢复性独立成立、已亲核），计入建议栏供 Gate 2 裁决时确认。

### Q2【未关闭，非阻塞】：`report_input.md` 在 `enabled=false` 时是否仍被无条件重写，新测试未覆盖此点是否算遗漏？

**可证伪点**：若 `export_comments` 在 `comments=[]` 时也跳过 `report_input.md` 重写（而非评审 #1 已确认的「无条件重写」），则 `test_cli_comments_respects_enabled_flag` 遗漏了这一半行为的回归锁。

**验证**：亲读 `src/core/exporter.py:258-260`，`paths["report_input"] = _write_report(...)` 在 `if comments:` 分支之外、无条件执行——`report_input.md` 确实仍会被重写（用评审 #1 「亲测记录」CHECK3 的独立验证结果一致：`report_input.md changed: False`，因为输入内容未变导致字节碰巧相同，但重写动作本身发生了）。新测试只断言了 `comments.csv` 不存在，未对 `report_input.md` 做二次断言，是**建议4 范围内的部分覆盖**而非缺陷——`_comments_stage`/`enabled` 交互的核心待裁决点是「是否采集」，测试已精确命中这一点；`report_input.md` 无条件重写是 §4 既定契约、非本次待裁决内容，未新增断言不算遗漏。**关闭**（不构成问题，仅记录辨析）。

---

## 建议栏（非阻塞，供后续参考）

1. 若用户 Gate 2 裁决要保留 `data/exports_real/` 数据，建议先确认废纸篓路径 `~/.Trash/xhs-recon-data-1783022861` 当下是否仍存在（本轮亲核为空），若已不存在则只能走 `git checkout e9640d1 -- data/exports_real` 这一条路径恢复。
2. `代码评审1.md` 在 `1e5d5ff` 里才补充归档进案例目录，与评审 #1 实际产出时间点不同步（正常情况下评审文档应在评审完成时即落盘），后续案例可考虑评审产出即时提交，避免和下一次修复 commit 混在一起，便于按 commit 边界追溯评审历史。

---

## 验证记录汇总

1. 独立复跑 `uv run pytest tests/test_artifacts.py tests/test_cli.py -q`：18 passed。
2. 独立复跑 `uv run pytest -q`：135 passed（133+2，与预期吻合）。
3. 独立复跑 `python3 workbench/tools/preflight.py --config workbench/cases/管线子命令拆分/checklist.yaml --repo .`：全绿 exit 0。
4. `git show 1e5d5ff --stat --summary` 亲核夹带范围：4 文件，`src/` 下零改动。
5. 亲读 `src/pipelines/artifacts.py:71-78`、`src/pipelines/cli.py:131-156`、`src/pipelines/run_research.py:257-266`、`src/core/exporter.py:240-261` 确认两条新测试的断言与实现路径逐行对应，非空断言/宽松匹配。
6. `git show e9640d1 --stat -- data/exports_real/` + `git show b2837ba --stat`：5 文件 797 行，与评审 #1、偏差5 登记三方一致。
7. `ls ~/.Trash`（0 项）+ `ls -la ~/.Trash/xhs-recon-data-1783022861`（exit 1）：偏差5 登记的废纸篓路径当前不可复核，计入质疑 Q1，不影响阻塞关闭判定。
8. `git status --short`：工作区干净，无残留改动。
